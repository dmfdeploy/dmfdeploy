# DMF Status

_Auto-generated 2026-06-11 09:15 UTC by `bin/generate-status.sh`. Run again to refresh._

For decisions, see [docs/decisions/](docs/decisions/INDEX.md).
For active task spec, see the most recent file in [docs/plans/](docs/plans/).
For most recent handoff, see [docs/handoffs/](docs/handoffs/).
For canonical architecture, see [docs/architecture/DMF Platform Plan.md](docs/architecture/DMF%20Platform%20Plan.md).

## Repo state

| Repo | Branch | Last commit | Subject | Dirty | Unpushed |
|---|---|---|---|---|---|
| dmfdeploy (umbrella) | main | `0cc5f6e` (5 minutes ago) | feat: working-model enforcement foundation, umbrel | 1: `.github/workflows/guard.yml` | **2** |
| dmf-cms | main | `d7016a8` (24 hours ago) | docs: drop stale 'spike' framing and neutralize op | clean | — |
| dmf-runbooks | main | `c84fde7` (25 hours ago) | docs: use "all repos" instead of a hardcoded count | clean | — |
| dmf-central | main | `a3ea74b` (25 hours ago) | docs: future-proof repo count and fix stale dmf-en | clean | — |
| dmf-infra | main | `0aa8e07` (25 hours ago) | chore: remove must-not-main MXL spike artifacts fr | clean | — |
| dmf-env | main | `eb91bda` (2 hours ago) | fix(doctor): make the age-key permission check por | clean | — |
| dmf-media | main | `654b054` (24 hours ago) | docs: drop stale '(SPIKE)' labels and neutralize o | clean | — |
| dmf-init | main | `4e46b57` (2 hours ago) | chore(release): v0.2.0 — console installer skin, w | clean | — |
| dmf-promsd | main | `d506e9c` (27 hours ago) | Initial public release v0.1.3 | clean | — |

## Active plans

| Plan | Date | Tracking issue |
|---|---|---|
| DMF Cluster-Internal Ansible Execution and Catalog Helm Pivot Plan 2026-05-19 | 2026-05-19 | — |
| DMF Dynamic NetBox-Driven Monitoring Plan 2026-06-04 | 2026-06-04 | [#5](https://github.com/dmfdeploy/dmfdeploy/issues/5) |
| DMF First Public Release Plan 2026-06-09 | 2026-06-09 | — |
| DMF OSS v0.1 WP0 Release Contract and Profile Matrix 2026-05-25 | 2026-05-25 | — |
| DMF OSS v0.1 WP1S Single-Node Sandbox Lane 2026-05-25 | 2026-05-25 | — |
| DMF OSS v0.1 WP5 Release Verification and Tagging 2026-05-25 | 2026-05-25 | — |
| DMF Working-Model Enforcement Plan 2026-06-11 | 2026-06-11 | [#32](https://github.com/dmfdeploy/dmfdeploy/issues/32) |
| DMF v0.1 Commitment & 30-Day Focus-Cut Plan 2026-06-06 | 2026-06-06 | — |

## Recent activity (last 7 days, all repos)

| When | Repo | Commit | Subject |
|---|---|---|---|
| 2026-06-11 11:10 | dmfdeploy | 0cc5f6e | feat: working-model enforcement foundation, umbrella side (refs #32) |
| 2026-06-11 10:11 | dmfdeploy | 21850d5 | docs: record sibling-repo layout post-publish (ADR-0001 amendment) ... |
| 2026-06-11 09:51 | dmfdeploy | 75ca986 | docs(plans): init installer UI redesign executed — shipped as dmf... |
| 2026-06-11 09:39 | dmfdeploy | a3cb740 | docs(plans): umbrella entrance plan executed — WP1-17 complete, r... |
| 2026-06-11 09:29 | dmf-env | eb91bda | fix(doctor): make the age-key permission check portable to GNU stat |
| 2026-06-11 09:23 | dmf-init | ca0ce11 | fix(ui): start the doctor run on arrival at Validate |
| 2026-06-11 09:23 | dmf-init | c1a1828 | feat(ui): within-step task ticker + per-step elapsed on the splash |
| 2026-06-11 09:23 | dmf-init | a8adc6e | fix(ui): workstation station — kill column overlap, ghosting and ... |
| 2026-06-11 09:23 | dmf-init | 4e46b57 | chore(release): v0.2.0 — console installer skin, workstation paus... |
| 2026-06-11 09:23 | dmf-init | 38e3f4d | feat(ui): console installer skin — left rail, wizard pages, viewp... |
| 2026-06-11 09:23 | dmf-init | 36ea345 | feat(ui): install activity ticker + step rail folded into sidebar |
| 2026-06-11 09:23 | dmf-init | 2f93395 | fix: fail fast on unusable SSH keys and never drop the render stream |
| 2026-06-11 09:23 | dmf-init | 28011b7 | fix(ui): map network-level fetch failures to operator-actionable er... |
| 2026-06-11 09:23 | dmf-init | 00ed35e | fix(ui): kill bootstrap-start stampede and Deploy-transition dead zone |
| 2026-06-11 09:19 | dmfdeploy | 7344326 | Initial public release v0.1.0 |
| 2026-06-11 09:07 | dmf-init | 8506456 | feat: recovery-package download with honest safe-to-delete gating |
| 2026-06-10 11:13 | dmf-media | 654b054 | docs: drop stale '(SPIKE)' labels and neutralize operator path |
| 2026-06-10 11:13 | dmf-cms | d7016a8 | docs: drop stale 'spike' framing and neutralize operator paths |
| 2026-06-10 10:53 | dmf-media | 26ab44b | chore: remove must-not-main MXL fabrics spike handoff docs |
| 2026-06-10 10:53 | dmf-infra | 0aa8e07 | chore: remove must-not-main MXL spike artifacts from public main |
| 2026-06-10 10:53 | dmf-infra | e01b590 | docs: correct GitHub org/repo model and refresh stale docs |
| 2026-06-10 10:53 | dmf-media | 36b3b10 | docs: future-proof repo count and fix stale dmf-env description |
| 2026-06-10 10:53 | dmf-cms | 9a71230 | docs: future-proof repo count and neutralize operator-local paths |
| 2026-06-10 10:52 | dmf-central | a3ea74b | docs: future-proof repo count and fix stale dmf-env description |
| 2026-06-10 10:32 | dmf-runbooks | c84fde7 | docs: use "all repos" instead of a hardcoded count in CLAUDE.md |
| 2026-06-10 08:26 | dmf-runbooks | 02b39a4 | Initial public release v0.1.3 |
| 2026-06-10 08:16 | dmf-infra | c591db7 | Initial public release v0.1.0 |
| 2026-06-10 08:16 | dmf-env | d96b968 | Initial public release v0.1.0 |
| 2026-06-10 08:15 | dmf-media | 29257aa | Initial public release v0.1.0 |
| 2026-06-10 08:15 | dmf-cms | fbe755e | Initial public release v0.10.0 |
| 2026-06-10 08:14 | dmf-promsd | d506e9c | Initial public release v0.1.3 |
| 2026-06-10 08:14 | dmf-central | de92278 | Initial public release v0.1.0 |

_dmf-init: 8 more commit(s) not shown._

## Operator notes (hand-edited — preserved across regenerations)

<!-- HUMAN-START -->
### 🚧 WORKING-MODEL ENFORCEMENT — umbrella foundation landed; 8-repo propagation NEXT (2026-06-11)
**Tracking: [issue #32](https://github.com/dmfdeploy/dmfdeploy/issues/32)** ·
plan `docs/plans/DMF Working-Model Enforcement Plan 2026-06-11.md` (reviewed by
claude-bottom, 5 points folded). Landed this session (umbrella): canonical
**`docs/WORKING-MODEL.md`** (with WM-DIGEST markers), block template
`docs/templates/working-model-block.md`, identical block in umbrella
CLAUDE/AGENTS/QWEN, **`bin/check-working-model-sync.sh`** (wired into ci.yml +
pre-commit, `--umbrella-only` so one repo's drift never blocks another),
**`bin/working-model-digest.sh`** + SessionStart hook in `.claude/settings.json`
(runtime-extracts the digest; warns-not-wedges if umbrella absent), INDEX +
CONTRIBUTING pointers. **NEXT (batch, all 8 component repos, PRs referencing
`dmfdeploy/dmfdeploy#32` qualified):** WP2 blocks in agent files + `--strict`
sync-check in each ci.yml, WP3 settings hook, WP4 guard.yml PR-must-link-issue
job (component repos REQUIRE qualified `Closes dmfdeploy/dmfdeploy#N`, reject
bare `#N`; `no-issue` label escape). **Operator-gated:** WP5 adjudicate the 6
active plans w/o tracking_issue then flip check-docs W2 warn→fail; WP6 hygiene
detector + schedule; WP7 org `.github` (optional).

### ✅ UMBRELLA PUBLISHED + SIBLING LAYOUT — docs/tooling updated (2026-06-11)
The umbrella is **live at `github.com/dmfdeploy/dmfdeploy`** (v0.1.0; WP17 push
completed by the operator) and the workspace layout flipped: component repos
are **siblings of the umbrella under a common parent** (`../dmf-*`), no longer
nested/gitignored inside it. Recorded as an **ADR-0001 amendment**; CLAUDE.md /
AGENTS.md / QWEN.md / README.md updated; `.gitignore` `dmf-*` entries kept only
as a legacy-nested-clone safety net. `bin/` tooling (generate-status,
generate-scripts-catalog, scrub/author/hygiene gates, export-scan,
mxl-sync-check) now resolves components in either layout (nested first, sibling
fallback) — before this fix STATUS.md reported every component "(not a git
repo)". CLAUDE.md backlog section now also names the milestones
(`v0.1-polish`/`v0.2`) + board Component/Workstream fields and the
`gh api graphql` project gotcha. Retired/mothballed tooling
(`bin/sync-to-github.sh`, `bin/agentic/`) deliberately untouched.

### 🚧 DMF-INIT INSTALLER UI REDESIGN — pass 1 done + Lima-validated, PR pending; pass 2 specced (2026-06-10)
**Canonical plan:** `docs/plans/DMF Init Installer UI Redesign Plan 2026-06-10.md`.
Pass 1 (installer-style create flow: Configure→Install→Connect→Finish→Validate,
modal pauses → inline stations, new `POST /api/bootstrap/doctor` endpoint) is
committed on dmf-init branch **`<handle>/installer-ui-redesign`** (3 signed-off
commits in the **github clone** `~/repos/dmfgithub/dmfdeploy/dmf-init`) and
**field-validated by a full operator Lima E2E run**. PR creation blocked on an
expired `gh` token — re-auth then `gh pr create` (body drafted in session).
**Pass 2 (specced, not started):** console-family skin (dmf-cms tokens + dmfdeploy
logo topbar, cyan accent kept), Calamares-style left step rail, Configure as a
4-page wizard with single Deploy button, install splash + honest N-of-M progress,
landscape viewport-fit (no page scrolling at 1280×800). **Backend deltas first:**
merge `ca-cert`+`hosts-map` pauses into one `workstation` pause; add
`GET /api/package/{env_id}` zip (backup #3 + CA + README + sha256) with
download-completion tracking gating "safe to delete". Field note: operator
enrolled a passkey from an iPad **without** CA trust — the "fails silently"
claim is desktop-Chrome-specific; CA station copy must be softened accordingly.

### 🚧 UMBRELLA PUBLIC ENTRANCE + GITHUB WORKING MODEL — WP1–WP11 executed (2026-06-10)
**Canonical plan:** `docs/plans/DMF Umbrella Public Entrance and GitHub Working Model Plan 2026-06-10.md`
(tracking: [issue #3](https://github.com/dmfdeploy/dmfdeploy/issues/3)).
**WP1–WP11 EXECUTED 2026-06-10** (Claude orchestrates+verifies, qwen-right lifts):
WP1 triage table + WP2 frontmatter on all plan docs (10 active/8 draft/64 executed/
9 superseded/25 historical) + WP3 READMEs/agentic-guards + WP4 `bin/check-docs.sh`
+ plans INDEX + WP5 STATUS active-plans/activity-cap + WP6 doc gate wired
(pre-commit + ci.yml; rejects bad frontmatter) + **WP7: umbrella repo =
`dmfdeploy/dmfdeploy`** + WP8 private repo scaffolded (labels/milestones/Discussions)
+ WP9/WP10 **TODOS→Issues #3–#26 on org Project #1** (5 drafts absorbed+deleted) +
WP11 TODOS.md retired to a pointer. Tracking issues minted: #3 entrance, #4
installer-UI, #5 monitoring, #7 init-build-workflows (frontmatter `tracking_issue:`
set). **WP12–16 EXECUTED 2026-06-10 (second session):** WP12 `docs/JOURNEY.md`
(first-person, full agent-transparency) + WP13 `docs/decisions/READING-GUIDE.md`
+ WP14 `docs/OPEN-QUESTIONS.md` (operator-approved) + WP15 front door (README
rewrite, `docs/THESIS.md`, `docs/INDEX.md` map, CLAUDE/AGENTS/QWEN + CONTRIBUTING
updates; fresh-eyes dry-run passed, findings folded in; wp1/wp9 tables flipped to
executed) + WP16 scrub sweep (qwen-right, ~90 files, grep-verified; residue fixed
incl. operator real-name class — patterns added to scrub gate; functional
functional public-handle mentions retained by adjudication; leaking `.qwen` skill untracked).
**WP17 PREPARED, NOT PUSHED:** `export-scan.sh .` works (orphan export at
`/tmp/dmf-export/dmfdeploy`, tag v0.1.0); GitHub metadata scan CLEAN. The
detection-pattern split landed 2026-06-11 (operator-approved): public-safe
`.gitleaks.toml` + gitignored `.gitleaks.local.toml` (pre-commit second pass)
+ scrub/dmf-env-gate identity/topology arrays sourced from
`~/.dmfdeploy/scrub-private-patterns.sh` (override `DMF_SCRUB_PRIVATE_PATTERNS`;
keep include + local toml in lock-step). Functional public-handle mentions
allowlisted by adjudication. **All export gates GREEN; push remains
operator-gated** (see publish-readiness handoff for the push procedure).
**Operator manual steps open:** add "RFC (pre-ADR)" Discussion category (no API
exists); pin discussion #27 (Announcements); add board Workstream options
catalog/identity-secrets/hardening/entrance (12 issues lack the field); decide on
2 proposed recovery issues (shared-UI-framework + init→cms convergence — rescued
from the `c0d45e6`×WP11 race; content safe in the installer-UI plan §Future
direction meanwhile). Gotcha: `gh project` subcommands 401 here — use raw
`gh api graphql` (project id `PVT_kwDOENb9uM4BaPY-`).

### ✅ POST-RELEASE DOC-HYGIENE — done (2026-06-10)
First public-repo doc-hygiene sweep complete. Two-agent audit (claude survey +
codex cross-check) → plan `docs/plans/DMF Public Repo Doc-Hygiene Cleanup Plan
2026-06-10.md` (+ exec/PR specs alongside). **7 PRs merged on GitHub as `<handle>`**
(rebase, lkirc-approved, auto-merge): `dmf-runbooks#1`, doc-hygiene `#1` on
dmf-central/dmf-cms/dmf-media/dmf-infra, and MXL spike-removal `#2` on
dmf-infra/dmf-media. Fixed: `lkirc`→`dmfdeploy`, `all 6 repos`→`all repos`,
README/structure/banners/links/paths. Removed the "NOT for main" MXL spike
playbooks/handoffs; **learnings preserved** in `docs/sessions/DMF MXL Cross-Cloud
Media-Node Spike — As-Built and Fabrics Result 2026-05-30.md`.
- **PR work happens in `~/repos/dmfgithub/dmfdeploy/<repo>`** (github origin) — NOT
  the `~/repos/dmfdeploy/<repo>` LAN clones. See memory `project_github_pr_clones`.
- **Remaining follow-ups:** MXL product-wording scrub (drop stale "SPIKE" framing
  on kept demo artifacts) + non-markdown path neutralization — in flight 2026-06-10.

### 🚧 FIRST PUBLIC RELEASE — in flight (2026-06-09)
Preparing the clean first public GitHub release. **Canonical handoff:**
`docs/handoffs/DMF First Public Release — C+B Done, A-D-E Next — Handoff 2026-06-09.md`
(read it first). Master plan + all locked decisions:
`docs/plans/DMF First Public Release Plan 2026-06-09.md`.
- ✅ **Workstream C** (ADR digests) — committed (umbrella `9dbdccb`/`0a8ba77`).
- ✅ **Workstream B** (dmf-env public scrub, Hetzner+sandbox only) — committed
  (dmf-env `1827085`, umbrella `641ebf1`). New gate: `bin/dmf-env-public-surface-gate.sh`.
- ✅ **Workstream D** (GitHub-first governance + DCO) — committed 2026-06-09 across all
  9 repos (umbrella `a198f92`; dmf-cms `10df2e2`, dmf-runbooks `b0f4a0c`, dmf-central
  `9db2a69`, dmf-infra `35b8efd`, dmf-media `0d5ae22`, dmf-init `293f683`, dmf-env
  `0fb6150`, dmf-promsd `f5d6107`). GitHub-first CONTRIBUTING/SECURITY/PR+issue
  templates + identical inline-`sh` DCO workflow; CODEOWNERS normalized to
  `@dmfdeploy/maintainers`; dmf-env/dmf-promsd brought to hygiene parity
  (LICENSE/NOTICE/VERSION/.gitleaks.toml/pre-commit); dmf-promsd Dockerfile source
  scrubbed; **Release & Contribution Model ratified as ADR-0041** (0018 was taken).
  Spec: `docs/plans/DMF Workstream D — GitHub-First Governance Execution Spec 2026-06-09.md`
  (codex-agreed, 4 rounds; grep-proof verified). **Commits are LOCAL-ONLY — NOT pushed**
  (dmf-runbooks especially: no push until E1 batches; dmf-init/dmf-promsd local sign-off
  is `Loz`/local — Workstream A's clean orphan import re-authors).
- ✅ **Workstream E1** (before-flip PR-gate CI) — committed 2026-06-09 across all 9 repos
  (umbrella `8b58421`; dmf-cms `2313bc6`, dmf-runbooks `186745b`, dmf-central `ee2391d`,
  dmf-infra `6a34c63`, dmf-media `6faa2de`, dmf-init `921ed45`, dmf-env `7b96916`,
  dmf-promsd `4a122c0`). Byte-identical `guard.yml` (gitleaks base-ref config + trivy
  vuln-only + inline Conventional-Commits + actionlint — **all inline SHA-pinned binaries
  w/ sha256, no composite actions**) + per-stack `ci.yml` (python-web/ansible/bash-tf/
  docs). Hardened: `pull_request` (not `_target`), `permissions: contents: read`,
  `persist-credentials: false`, all `uses:` 40-hex-pinned, hosted runners only. **actionlint
  clean across all 18 files;** qwen's trivy/gitleaks checksums verified vs upstream; I fixed
  qwen's one botch (added the real OpenTofu sha256 in dmf-env ci.yml). Spec:
  `docs/plans/DMF Workstream E1 — PR-Gate CI Execution Spec 2026-06-09.md` (codex-agreed,
  3 rounds). **LOCAL-ONLY — NOT pushed.** dmf-init appliance-bundle build EXCLUDED (secrets/
  self-hosted → E2 + 14GB-disk spike).
- ✅ **A — FIRST PUBLIC RELEASE COMPLETE (2026-06-10).** All **8 component repos PUBLIC,
  clean, protected, CI-GREEN** at `github.com/dmfdeploy/<repo>`: dmf-central `v0.1.0`,
  dmf-cms `v0.10.0`, dmf-infra `v0.1.0`, dmf-media `v0.1.0`, dmf-init `v0.1.2`, dmf-env
  `v0.1.0`, dmf-promsd `v0.1.3`, dmf-runbooks `v0.1.3`. Each = **1 clean orphan commit**
  (author `<handle>` noreply, signed-off), current-VERSION tag, **guard+ci CI SUCCESS**,
  secret-scanning + push-protection on, **3 rulesets** (branch + 2 tag, no bypass),
  rebase-merge-only. **umbrella (now named `dmfdeploy/dmfdeploy`, WP7 2026-06-10; was
  dmf-platform) DEFERRED to B2 doc-scrub.** Specs:
  `docs/plans/DMF Workstream A — Clean-History Import and Canonical Flip Execution Spec
  2026-06-09.md` (+ Pre-A Tooling + CI Recovery Fix specs). **Tooling:** `bin/export-scan.sh`
  (clean orphan + scan exact tree under **pinned gitleaks 8.21.2**, NEVER pushes;
  recreate via `gh` + SSH key `~/.ssh/forgejo_loz`); sync-to-github.sh RETIRED.
  - **CI-recovery event (2026-06-09→10):** first flip went red — harness used wrong gitleaks
    (8.30.1 vs CI's 8.21.2 → version skew), qwen's actionlint sha256 was wrong, dmf-cms had a
    `Owner: Loz` miss, + per-stack CI bugs never run on real runners. Recovery (codex multi-
    round): pinned harness to 8.21.2, fixed actionlint sha256, sanitized dmf-infra forgejo-
    chart private-key placeholders, yamllint `.yamllint` relaxed + ignore Helm templates,
    ansible-lint `.ansible-lint profile:min`, dmf-cms react-router 7.17.0 (CVE) + StaticFiles
    check_dir=False + base_path-aware fallback redirect + "App Catalog" + Owner fix, dmf-init
    age+rclone, dmf-env yq→jq (runner yq v4) + sops/tofu fmt/shellcheck. Operator deleted all
    8 + I recreated clean. Handoff: `docs/handoffs/DMF First Public Release — CI Recovery +
    Reimport 2026-06-09.md`.
  - **Deferred follow-ups:** Forgejo retire-to-archive + `<repo>-archive` repos + origin
    repoint (history safe meanwhile — LAN Forgejo + local clones retain it); umbrella B2.
  - `lkirc` added to `dmfdeploy/maintainers` (PR review now possible). ✅ **2
  operator decisions RESOLVED (2026-06-09):** (1) tag at **current VERSION per repo** (cms
  v0.10.0, promsd v0.1.3, init v0.1.2, rest v0.1.0); (2) **umbrella deferred to B2**. **A is
  irreversible/outward — per-repo operator go-ahead required; nothing executed.** **E2**
  (release automation) also NOT started.
  ✅ **CODEOWNERS team prereq RESOLVED (2026-06-09):** `dmfdeploy/maintainers` exists,
  privacy `closed` (org-visible), `<handle>` active maintainer, and granted
  **All-repository Write** at the org level → covers all current + future repos (no
  per-repo grant needed in A). Verified `dmf-runbooks` = `push=true, admin=false` (Write
  only; admin stays at org-owner level). ✅ **Merge method DECIDED (operator, 2026-06-09):
  rebase-and-merge ONLY** (disable squash + merge-commit per repo) → DCO `Signed-off-by`
  survives + linear history. Wired into Workstream A per-repo setup. **For E1/A:**
  `.githooks/pre-push` is missing in ALL repos (the hygiene gate never passed clean) —
  add everywhere or relax the gate.
  ✅ **LAN Forgejo push-mirror hazard RESOLVED (operator, 2026-06-09):** the
  Forgejo→GitHub **push-mirror job for `<handle>/dmf-runbooks` was removed.** dmf-runbooks D/E1
  commits stay local but pushing to Forgejo no longer auto-leaks to GitHub. Workstream A
  retire-to-archive must still confirm no Forgejo→GitHub push mirror on the other repos as
  their GitHub repos are created.
- Model: **GitHub-canonical-forward**, Forgejo retire-to-archive, Free-tier locked-window
  flip, DCO, dmf-promsd publishes, dmf-env keeps its name. Orchestrate qwen(lift)+codex(check).

### 🟢 NetBox-driven monitoring — FROM-SCRATCH BOOTSTRAP VALIDATED GREEN (2026-06-05)
Ran a clean from-scratch sandbox bootstrap (dmf-init → env **`zmlj-sikn`**, Lima VM
`<sandbox-node-ip>`) to live-validate the token fix. **All gates green, independently
verified:**
- **G1** OpenBao `secret/apps/netbox/runtime#promsd_api_token` non-empty (len 57).
- **G2** ESO `monitoring/dmf-promsd-netbox` synced, `netboxToken` len 57.
- **G3** adapter authenticates from the ESO secret — anon NetBox=403 vs token=200/count=10
  — **no `kubectl set env`** (the thing that failed last run).
- **G5** `/sd/probe` = 10 svc-DNS target groups; Prometheus `netbox-probe` **10/10 up**.

The run surfaced a **second, separate first-bootstrap bug** (only a true cold start
shows it): **born-inventory create-path id-loss** — `Create ipam.Service` response
was never registered, so the stamp tasks skipped and `cluster_*` stayed NULL →
empty probe. Fixed in `dmf-infra` **`553efd3`** (register the create id), qwen-reviewed,
verified via a *forced create-path* (deleted the 10 services, re-ran 694 → 10/10 up).

**Pushed:** `dmf-infra` `abb1d0a..553efd3` (token persist + 4-mint sentinels + born-inv).
**Minor open:** grafana(HTTP 0, `/grafana` subpath) + loki(HTTP 404, no `/`) fail the
`http_2xx` root probe — per-app probe-path tuning; discovery itself is correct.
**Sandbox `zmlj-sikn` left UP** (VM `<sandbox-node-ip>`). Orchestrated codex(drive + hi-reason
diagnosis) → qwen-left(review) → Claude(verify each gate). Specs in `docs/plans/`.

### 🟢 NetBox-SoT token-mint hardening — pushed (2026-06-04, orchestrated)
Orchestrated lift→review→fix on `dmf-infra` (codex lift, qwen-left review, Claude verify):
**pushed `18d3e95..abb1d0a` to main.** Two fixes in `netbox-sot/tasks/main.yml`:
- `17a4a62` — **4-mint sentinel hardening** (admin/awx/librenms/catalog) using the FIX5
  sentinel-capture pattern (robust against `manage.py` banner lines). TODOS item closed.
- `abb1d0a` — **finding #5 root cause + fix**: the OpenBao persist task piped the PromSD
  token into the pod via `printf` but had **no matching `read`**, so `promsd_api_token`
  persisted **empty** (→ ESO empty → hand-injected live). Same off-by-one was corrupting
  the **librenms** token. Fix adds `IFS= read -r PROMSD_TOKEN` in printf-order. codex's
  first attempt was a no-op (swapped one pod-invisible var for another); the review lane
  (qwen-left + my verify) caught it; round 2 amended correct.
- **Live-validated 2026-06-05** (see top note, env `zmlj-sikn`): token → OpenBao → ESO
  projection → adapter, with no `kubectl set env`. G1–G3 green.
- Also confirmed this session: `ghcr.io/dmfdeploy/dmf-promsd:0.1.3` is **public** +
  multi-arch (amd64/arm64), anon-pullable → the canonical 630 mirror path is unblocked.
- Task spec: `docs/plans/DMF NetBox-SoT Token-Mint Hardening Task 2026-06-04.md`.

### 🟢 NetBox-driven monitoring — built + live e2e green (2026-06-04)

The full **NetBox-driven dynamic monitoring** feature (WP0–WP8 + **ADR-0038 incl.
Amendment A**) shipped and was **validated live end-to-end** via a from-scratch
dmf-init bootstrap on the `montest` sandbox (env `8f2y-sgg7`): Prometheus now probes
**10 platform apps discovered from NetBox tags, all `up`**. New component repo
**`dmf-promsd`** (the http_sd adapter); image published → `ghcr.io/dmfdeploy/dmf-promsd:0.1.3`
(**operator: flip the package to public**). **8 deploy-time findings** surfaced — 7 fixed
on `main`, the 8th became **Amendment A** (probe/scrape targets via stable cluster
svc-DNS, not pod/ingress IPs; the deployer records `cluster_service/namespace/port`).
Orchestrated codex+qwen-right (lift) → Claude (verify) → qwen-left (review).
- **Env torn down** after validation (all VMs stopped). **Recommended next: start fresh**
  — a clean from-scratch bootstrap now exercises every fix with no workarounds.
- **Handoff (canonical):** `docs/handoffs/DMF NetBox-Driven Monitoring — Build + Live e2e Handoff 2026-06-04.md`.
- **Open follow-ups:** validate the ESO token path (#5, was injected live), WP6 catalog
  launcher cluster-coord stamping (dynamic-service e2e), and the 4-mint sentinel hardening.

### 🟢 Converge-on-main + releases: dmf-cms 0.10.0 + dmf-init 0.1.2; feat branches retired (2026-06-03)

Policy locked: **pre-public/solo → everything on `main`, no feature branches.**
- Retired **`feat/mxl-spike`** across all 6 repos (it was fully contained in `main`;
  worktrees + Forgejo refs removed) and **`feat/wizard-non-interactive`** (dmf-env; == `main`).
- **dmf-cms `0.10.0`** released → `ghcr.io/dmfdeploy/dmf-cms:0.10.0` (public, anon-pullable):
  ships the MXL Flows page as a real image, retiring the `915-mxl-cms-override` dev-hack.
- **dmf-init `0.1.2`** built + published → `ghcr.io/dmfdeploy/dmf-init:0.1.2`: `DEFAULT_REFS`
  now point **all** runtime repos (incl. dmf-env, was on the feat branch) at `main`, so a fresh
  Create clones `dmf-cms@main` (=0.10.0) and 630/650 resolve `cms_image_tag` natively.
- **Media Workloads** model recorded: **ADR-0037** (amends ADR-0027 — instances in NetBox,
  not a CRD; flows stay runtime-only) + `docs/plans/DMF Media Workloads … 2026-06-03.md`.
- **Clean restart:** disposable `fl21-cbq0` abandoned; old `dmf-init-demo` container removed;
  `dmf-sandbox` Lima VM recreated (fresh). dmf-init now runs on a **dedicated `dmf-init`
  colima profile** (2CPU/4GiB/40GiB), not `default`.
- **Next (operator):** fresh dmf-init Create against the new VM → bootstraps latest `main` +
  dmf-cms 0.10.0 (630 mirror + 650 deploy run inside post-seed).

### 🟢 dmf-init BOOTSTRAP-PAUSE UX — delivered + full E2E green (2026-06-03)

The three bootstrap pauses (ca-cert/hosts-map/passkey) shipped empty payloads; now
**live-fetched + actionable in the UI**. Handoff: `docs/handoffs/DMF dmf-init
Bootstrap-Pause UX Tasks Handoff 2026-06-03.md`. Orchestrated codex(lift)→Claude(verify,
incl. live builder checks)→qwen(review); pushed `dmf-init main` `5b41950..7456014`
(T1 payloads+metadata, T2 UI per-OS CA cmds+download/hosts+DNS note/clickable enroll
link/per-pause Verify&Continue, T3 `GET /api/bootstrap/passkey/{run_id}`).
**Full from-scratch E2E (env `fl21-cbq0`, fresh blank VM, operator drove it in the
browser): all 11 steps DONE → "Bootstrap verified, #2+#3 sealed", verify failed=0**;
pauses completed via the new UI; passkey Verify&Continue gated on the live poll.
**#9 RESOLVED:** ran `@main` with NO sidesteps and configure passed `ok=694 failed=0`
(dmf-runbooks merge `7d9e8b9` supplied the mxl launcher playbooks). Follow-up findings
(dmf-init backlog): backup returns raw 500 on bad rclone remote (should be clean 4xx);
form re-submit creates duplicate orphan envs; two-click start is non-obvious. Deferred
(NOT dmf-init): dmf-cms `0.9.2` `add_user_to_group` crashloop persists (masked by fallback
pod + weak `699` smoke test) → ships with the dmf-cms version bump.
**Manage TEARDOWN action also validated (with a fix):** drove it via the UI on `fl21-cbq0`
— lock + re-backup (cp#4 sealed) worked, but `remove-env.sh` refused the rm-rf because its
guards hardcoded `~/.dmfdeploy/envs/` (didn't honor `DMF_DATA_ROOT`, same family as #1).
FIXED+pushed dmf-env `feat/wizard-non-interactive` `c43401b` (DMF_DATA_ROOT-honoring base,
safety kept, +test); re-ran live → env removed. **#12 still open** (sandbox teardown doesn't
decommission the node). Oddity: remove-env runs cloud-lane "Delete Hetzner SSH key" even on
sandbox — confirm true no-op off-cloud.

### 🟢 dmf-init Live-Sandbox FIX BATCH — #5/#7/#2/#3/#10 fixed + pushed (2026-06-03)

Orchestrated batch (codex lifted → Claude verified pytest/shellcheck → qwen-left
reviewed each diff). Work order: `docs/plans/DMF dmf-init Live-Sandbox Fix Batch Plan
2026-06-03.md`. **All pushed to the LAN Forgejo (`<lan-ip>`):**
- **#7** dmf-init `main` `a1bfe4c` — repo-fetch now includes `dmf-cms` + `dmf-media`.
- **#5** dmf-env `feat/wizard-non-interactive` `d078c67` — `unseal-openbao.sh` honors the
  env SSH key (`-i` via `ssh_opts` array + `OPENBAO_SSH_KEY` override); bonus latent jq
  `.sealed // "?"` bug fixed. seed-bao audited (already passes `-i`).
- **#2/#3/#10** dmf-init `main` `51a64e7` — SSH key + derived `.pub` now land in
  `envs/<id>/ssh/` (so they ride the backup), inventory + answers paths rewritten to the
  env dir, and Manage restore rewrites them to the **relocated** env dir on
  restore-into-a-different-`DMF_DATA_ROOT`. New restore round-trip test proves it.

NOTE: dmf-init got its **first git remote** this session (operator created
`<handle>/dmf-init` on the LAN Forgejo; `origin = git@<lan-forgejo-ssh-alias>:<handle>/dmf-init.git`,
`main` tracks origin). The Phase-1c "NOT pushed — still no remote" note below is now stale.

**Still open:** **#9** (catalog AWX JT 400) + **#11** (MXL Console) are platform/data,
not dmf-init code — a clean main-only re-run needs the `mxl-videotestsrc` catalog entry
dropped (or dmf-runbooks at a ref with the launcher) + the MXL dmf-cms dev image.
Follow-up nit DONE: duplicated `_rewrite_yaml_scalar` harmonized into a shared
`src/dmf_init/yaml_utils.py` (dmf-init `9e95694` + regression-fix `5b41950`, pushed) —
both paths now quoted; the previously skip-gated createnew e2e render path was executed
for real (`DMF_TEST_DMF_ENV_REPO=<dmf-env checkout>`) and passes. dmf-env
`feat/wizard-non-interactive` is PR-able to main.

### 🟢 dmf-init FIRST LIVE dmf-sandbox EXERCISE — bootstrap+manage proven, 10 findings (2026-06-03)

First live run of dmf-init 1b+1c against a real `dmf-sandbox` VM
(`<sandbox-node-ip>`, `*.dmf.test`, user `<operator>`). App run locally (`uv run`, host
toolchain), repos via `file://`, backups to two local rclone remotes. **dmf-init
drove the VM from blank Debian through a FULLY installed platform** (k3s,
cert-manager, OpenBao, ESO, authentik, netbox, forgejo, AWX, dmf-cms, prometheus,
loki, grafana) + 333 configure tasks, plus the **full Manage restore→doctor→lock→
drift** cycle. Details: `docs/handoffs/DMF dmf-init Live-Sandbox Exercise Findings
2026-06-03.md`.

**Validated live:** one-shot launch token (reuse→403), repo-fetch, render,
checkpoint #1, pre-seed (309 tasks), **checkpoint #2 OpenBao-key capture at the
expected path + redaction + dual-remote re-backup** (the big §2 unknown — holds),
unseal, seed-bao, post-seed (full app stack), configure (333 tasks), and Manage:
restore (inner-sha verified), doctor (12/12), lock acquire (non-identifying
`container` tag — not hostname), drift DETECTION (refused a planted newer push).

**Fixed + committed (3):** dmf-env `437b8af` #1 `_resolve_env_paths.sh` honor
`DMF_DATA_ROOT`; dmf-init `f5616c2` #6 unseal exit-2 tolerance + #8
`RUNBOOK_TIMEOUT=5400`.

**UNFIXED follow-ups (diagnosed in the findings handoff):**
- **#2/#3/#10 createnew SSH-key (the big one):** inventory bakes the pre-move temp
  key path; no `.pub` derived; key lives in `runs/` (absolute, NOT in the backup)
  → Manage restore to a fresh container has no node SSH key. Fix = write key+`.pub`
  into `envs/<id>/ssh/` (backed up + portable) + stable inventory reference.
- **#5 dmf-env `unseal-openbao.sh`/seed-bao** use bare `ssh` (no `-i`), ignoring
  the env key → can't reach the node without the operator's agent (a stateless
  container has none). Fix = pass `-i <ansible_ssh_private_key_file>`.
- **#7 dmf-init repo-fetch** missing `dmf-cms` AND **`dmf-media`**. dmf-media is
  the **catalog source** the DMF Console facility/workflow/catalog page is built
  from (dmf-media/catalog → cluster Forgejo → AWX project → NetBox → Console);
  unfetched → Console catalog empty. Fix = add both to `REPO_NAMES`.
- **#9 (platform, not dmf-init):** configure's AWX catalog
  `media-launch-mxl-videotestsrc` JT → 400 (mxl launcher absent from the synced
  project); blocks reaching the bootstrap pauses + verify + checkpoint #3 live.
- **#11 MXL additions absent from the Console (compound):** #7 (dmf-media
  unfetched) + #9 (mxl JT 400) + post-seed deploys released dmf-cms `0.9.2` not
  the MXL dev-image (915 SPIKE override). Local repos confirmed up-to-date w/
  origin/main (not staleness). See findings handoff + [[project_mxl_spike_state]].
- **Not yet live-tested:** bootstrap pauses (ca-cert/hosts-map/passkey) resume +
  verify + checkpoint #3 (behind #9); Manage actions (behind #10).

### 🟢 dmf-init Phase 1c COMPLETE (hermetic) — Manage mode COMMITTED (2026-06-02)

Phase 1c builds the **Manage** lifecycle (manage an already-commissioned env from
a fresh container): restore → read-verify-before-lock → remote lock → actions →
re-backup. Built **hermetically** (mock command executor + local rclone remotes;
the live `dmf-sandbox` exercise of 1b+1c stays the deferred operator-gated
follow-up). **4 commits on dmf-init `main` (NOT pushed — still no remote):**
`56678df` 1c.1 + `ba36f8f` 1c.2 + `e9a989c` 1c.3 + `b5fa77b` 1c.4. Design +
slicing: `docs/plans/DMF Init Phase 1c Manage Mode Design 2026-06-02.md`
(qwen-left reviewed TWICE — design CHANGES-NEEDED → folded; 1c.2 code
CHANGES-NEEDED → folded).

- **1c.1 restore + doctor (`manage.py`):** wraps `backup.restore()` → relocate
  env into the canonical tmpfs layout **by copy BEFORE `cleanup()`** →
  reconstruct `render.json` from the manifest (node_ip/base_domain null — no
  action path reads them) → `build_doctor_run` (reuses the orchestrate engine,
  seeds redactions from openbao-keys.json). Endpoints `/api/manage/restore`
  (422 wrong-passphrase / 409 integrity / 502), `/doctor`, DELETE session +
  TTL GC. Best-effort tmpfs WARN on `$DMF_DATA_ROOT`.
- **1c.2 lock + drift (`manage_lock.py`) — HIGH RISK, double-reviewed:** advisory
  remote lock on dest-remote[0] (`MANAGE.lock`): acquire = read → 5-min-skew
  expiry → copyto → 3s settle → read-back refuse-on-different-holder;
  release/revalidate only-if-ours. Drift = **list-prefix → newest-by-stamp →
  decrypt-manifest → compare inner_sha256** (NOT re-fetch own name). qwen code
  fixes folded: check_drift differentiates decrypt-vs-other (never falsely claim
  "newer backup exists"); **malformed-lock auto-recovery via remote mtime**
  (rclone lsjson, tz-correct parse) only past ttl+tolerance; **container tag is
  non-identifying** (`dmf-<token>`, never the operator hostname); expired=None for
  malformed. `DMF_MANAGE_LOCK_TTL_SECONDS` (3600).
- **1c.3 actions + re-backup (`manage_actions.py`):** restore is lock-free; lock
  is a separate "begin managing" step gated by the drift check. Mutating action =
  `[command, rebackup-checkpoint]`; **teardown = `[rebackup, remove-env]`
  (backup BEFORE destroy)** — backup() needs env_dir. `run_action_then_release`
  thread wrapper releases the lock on terminal (success OR failure); teardown
  finalizes + wipes the session. Endpoints `/api/manage/lock/{acquire,release,
  state}` + `/action/start` (require locked + revalidate; lapsed→409). Monotonic
  checkpoint numbering. rerun-playbook path-traversal-guarded.
- **1c.4 frontend (`ManageView.tsx` + `ui.tsx`):** Create/Manage mode switch
  (Create-new flow preserved); 3-phase restore→restored→managing; restore form,
  verified-state panel (env_id/profile/checkpoint/provenance), doctor stream,
  lock banner + acquire/release, action buttons (rerun-playbook/upgrade/rotate/
  teardown w/ type-to-confirm), streamed `ManageConsole` reusing the bootstrap
  stream endpoint (no pause handling). Shared UI extracted to `ui.tsx`.
- **Every commit:** ruff clean, pytest green (34→41 incl. real rclone+age
  round-trips + lock/drift/action tests), gitleaks clean, frontend `npm run
  build` (strict tsc) clean. Orchestrated by claude-bottom, lifted by codex
  (fresh-cleared per slice), qwen adversarial review on the 1c design + the 1c.2
  lock/crypto code.
- **DEFERRED (next, operator-gated):** the **live `dmf-sandbox` exercise** of
  1b+1c against a real env (confirms openbao-keys.json path, tier-3 unseal, real
  CA-cert/enrollment payloads, lock+drift against a real remote, restore→doctor
  ordering). Then Phase 2 (cloud/Hetzner: provider creds + B2 + TF state in the
  backup; Shamir-collapse ADR). Tracked: createnew run/stream DRY; push dmf-init
  once the operator creates the LAN Forgejo repo. Handoff:
  `docs/handoffs/DMF dmf-init Phase 1c Complete + Live-Sandbox Follow-Up Handoff 2026-06-02.md`.

### 🟢 dmf-init Phase 1b COMPLETE (hermetic) — bootstrap orchestration COMMITTED (2026-06-02)

Phase 1b builds the full bootstrap orchestration on top of 1a's Create-new core.
Built **hermetically** (mock command runner; the live `dmf-sandbox` exercise is
the deferred follow-up — VM was stopped). **3 commits on dmf-init `main` (NOT
pushed — still no remote):** `96fc93f` 1b.1 engine core + `2efba64` 1b.2 real step
graph + checkpoint #2/#3 + `78313bd` 1b.3 React bootstrap view. Design +
slicing: `docs/plans/DMF Init Phase 1b Orchestration Design 2026-06-02.md`
(qwen-left-reviewed, CHANGES-NEEDED → all folded).

- **Engine (1b.1):** `orchestrate.py` — typed steps (Command/Checkpoint/Pause),
  `BootstrapRun` with a **canonical append-only event log + threading.Condition
  cursor** (NO queue.Queue), single atomic `emit()` (redaction under the lock),
  pre-created pause Events, `wipe_secrets()` on terminal, no secret-bearing repr.
  Endpoints: `/api/bootstrap` start | stream/{run_id}?from= | resume | DELETE +
  run-registry TTL GC. Folds qwen design review (3 P1/3 P2/1 P3).
- **Step graph + checkpoints (1b.2):** `bootstrap_steps.py` — the real sandbox
  graph (pre-seed → **checkpoint-2** → unseal → seed-bao → post-seed → configure
  → ca-cert/hosts-map/passkey pauses → verify → **checkpoint-3**). Checkpoint #2
  reads `<env_dir>/openbao-keys.json`, adds every secret value to the redaction
  set **before** unseal/seed-bao stream, then re-runs `backup()`; **hard-fails
  (terminal error) if the keys file is missing** (won't stream unredacted
  secrets). `backup.py` gained an optional `checkpoint` manifest field. qwen
  adversarial secret-capture review: CHANGES-NEEDED → all folded (P1 missing-keys
  abort, P2-1 passphrase off ctx / run owns it, P2-2 min-length redaction guard).
- **Frontend (1b.3):** `BootstrapView.tsx` + `ndjson.ts` — live console with a
  step rail + checkpoint badges, server-redacted log stream, cursor-based
  reconnect, and a locked pause modal (CA-cert download / hosts-map copy / passkey
  link). Wired after checkpoint #1; passphrase/remotes reused from state.
- **Every commit:** ruff clean, pytest green (18, incl. hermetic E2E + checkpoint
  round-trip), gitleaks clean (private-IP rule intact), arm64 image builds, frontend
  `npm run build` (strict tsc) clean. Orchestrated by claude-bottom, lifted by codex
  (fresh-cleared per slice), qwen adversarial review on the 1b.2 secret-capture path.
- **DEFERRED (next):** live `dmf-sandbox` exercise of the real pre-seed→verify
  sequence (confirms the openbao-keys.json path, tier-3 unseal, real CA-cert /
  enrollment-URL payload sources, pause ordering); then **1c Manage mode**
  (restore → read-verify-before-lock → remote lock → actions → re-backup).
  Tracked: createnew run/stream DRY. Handoff:
  `docs/handoffs/DMF dmf-init Phase 1b Complete + Live-Sandbox Follow-Up Handoff 2026-06-02.md`.

### 🟢 dmf-init Phase 1a COMPLETE — Create-new (render + dual-remote passphrase backup) COMMITTED (2026-06-02)

The disposable-after-commission core works end-to-end. **4 commits on dmf-init
`feat/phase1-foundation` (NOT pushed, not merged):** `a27892e` 1a.1 thin
container + `99ebb39` 1a.2-i runtime repo-fetch + session-enforcement + `b355094`
1a.2-ii-a backup/restore crypto + `c237775` 1a.2-ii-b Create-new render +
checkpoint #1. Architecture = ADR-0036 (thin control container; repos cloned at
runtime at a selected ref; app images pulled from GHCR at bootstrap; nothing
baked). Plan: `docs/plans/DMF Init Phase 1 Implementation Plan 2026-06-02.md`.

- **Create-new flow (sandbox lane, no apply yet):** loopback FastAPI + single-use
  launch token → session; fetch dmf-env/dmf-infra/dmf-runbooks at an
  operator-selected ref into tmpfs (LAN Forgejo + runtime creds, never baked/
  logged/persisted to .git/config); age-keygen → inputs-only answers-file →
  invoke the FETCHED `init-wizard.sh --non-interactive` → render → **checkpoint
  #1**: passphrase-wrapped (pyrage age-scrypt, in-memory) tar of {env dir + age
  key + answers + MANIFEST} pushed to TWO rclone remotes (overwrite-validated);
  restore verifies inner_sha256 + exposes the age key.
- **Proven via a hermetic E2E (no cluster):** fetch → render a sandbox env →
  dual-remote backup → restore → `bootstrap-secrets.sh doctor` PASSES. = the
  plan's 1a.2 acceptance.
- Orchestrated by claude-bottom, lifted by codex (fresh-cleared per slice), qwen
  adversarial crypto review on the backup module (1 P1/3 P2/2 P3 all folded:
  PTY fallback removed, restore-cleanup contract, answers-file integrity guard).
  claude caught/fixed several codex-can't-verify bugs (pyrage recipient parse +
  hard-dep, JSON-formatter key-redaction, render answers ssh-key path-vs-contents).
  Every commit: ruff clean, pytest green (9/9 incl. E2E), gitleaks clean (incl.
  the repo's private-IP rule), arm64 image builds (prebuilt pyrage aarch64 wheel).
- **Deferred (tracked):** run/stream render are ~95% duplicated → DRY later.
- **Next: Phase 1b** — bootstrap orchestration (pre-seed → checkpoint #2 capture
  OpenBao unseal key → unseal → seed-bao → post-seed → configure → verify) with
  streamed logs + UI pause-points + checkpoint #3.

### 🟢 dmf-init Phase 1a.1 — thin control-container foundation COMMITTED (2026-06-02)

Architecture pivot during 1a.1, locked as **[ADR-0036](docs/decisions/0036-dmf-init-thin-control-container.md)**:
dmf-init is a **thin control-plane container** — bakes ONLY the tool layer + the
FastAPI/React app; **no repos, no app images**. Playbooks are `git clone`d at
**runtime at an operator-selected ref** (LAN Forgejo + operator creds now →
public repos + release tags later; ref+SHA recorded in the backup manifest =
"versioned to match releases"). App images keep coming from **GHCR+upstream at
bootstrap** (mirrored to Zot by 630). This makes the image **public-safe from day
1** (nothing private baked) — the earlier "bake the repo layer / IP-scrub /
operator-internal image" idea is **dropped**. Air-gap (a portable image bundle)
is deferred. Plan: `docs/plans/DMF Init Phase 1 Implementation Plan 2026-06-02.md`
(qwen-left-reviewed; 5 P1/9 P2/3 P3 folded).

**dmf-init `a27892e` on branch `feat/phase1-foundation` (NOT pushed, not merged):**
FastAPI (127.0.0.1 bind, single-use launch token → cookie → token-free redirect,
JSON logging + key-aware secret redaction, /healthz), Vite/React/TS/Tailwind
landing, multi-stage arm64 Dockerfile (tool layer pinned: tofu 1.10.3 / sops
3.10.2 / kubectl 1.33.1 / rclone 1.69.1 + ansible/age/git/jq/openssh), pytest,
.forgejo CI (ruff+pytest / npm build / docker-build). Orchestrated by
claude-bottom, lifted by codex, reviewed by claude. **Verified:** ruff clean,
pytest 5/5, gitleaks clean (incl. the repo's private-IP rule), arm64 image
builds, /healthz 200 on 127.0.0.1, launch token to stdout, full tool layer
resolves in-container.

**Open for 1a.2:** session-enforcement on protected routes (token is currently
exchange-only); the **runtime repo-fetch** module (clone repos at selected ref
into tmpfs); render via `init-wizard.sh --non-interactive` + **dual-remote
passphrase backup** (backup/crypto gets a qwen adversarial review before commit).

### 🟢 dmf-init Phase 0 — `init-wizard.sh --non-interactive` LANDED + PUSHED (2026-06-02)

First slice of the dmf-init bootstrap container (plan
`DMF Init Bootstrap Container Plan 2026-06-02.md`): the env wizard can now be
driven by a YAML answers file — the shared contract for the future dmf-init web
UI + CI — while the interactive CLI stays first-class (both modes share one
`validate_inputs` + one `render_tail`). **dmf-env commit `2a75774` on branch
`feat/wizard-non-interactive`, PUSHED to LAN Forgejo (origin), NOT merged to
main** (PR: http://<lan-forgejo-host>/<handle>/dmf-env/compare/main...feat/wizard-non-interactive).

- `init-wizard.sh --non-interactive <answers.yaml>` + `--help`; `--remove`
  passthrough intact. Non-interactive auto-confirms + stops at artifact
  generation for all lanes (no `validate-env.sh`; dmf-init orchestrates that).
- Answers file = YAML (parsed via `yq`), **inputs-only**: the ~10 generated
  secrets + env_id + per-env SSH keypair stay wizard-internal/random; SSH keys
  referenced by path (or "generate"), never embedded. Schema:
  `dmf-env/docs/answers-file-schema.md`.
- Hermetic parity test `dmf-env/tests/wizard-noninteractive-parity.sh` (sandbox
  structural parity + hetzner render smoke; never asserts secret byte-equality).
- Orchestrated by claude-bottom, lifted by codex (3 rounds), reviewed by claude
  + qwen-left. Review caught a POSTURE_* unbound-var crash on the cloud lane
  (fixed → shared `apply_posture_profile`) + a qwen defense-in-depth/test/cleanup
  pass. Verified: bash -n, parity PASS, shellcheck clean (only pre-existing
  findings), gitleaks no leaks. **Structurally validated only — no live env run.**
- Next: dmf-init Phase 1 (the container itself — image + FastAPI/React skeleton
  + render + dual-remote passphrase backup).

### 🟡 Env-lifecycle refactor (ADR-0035) CODE-COMPLETE + PUSHED, NOT YET LIVE-VERIFIED (2026-06-01)

Big dmf-env refactor: **every env (cloud + sandbox) is now fully operator-local**
under `~/.dmfdeploy/envs/<env>/`; dmf-env is **generic** (scripts + `terraform/modules/`
+ generic per-provider root `terraform/hetzner/` + neutral tasks). **Per-env SSH keys**
generated by the wizard (privkey in the sops bundle, materialized to a 0600 file at
run time by run-playbook); `prevent_destroy`/`maybe_import`/reuse-by-fingerprint all
removed (those caused the apply deadlock). Wizard does everything up to — not incl. —
`tofu apply` (`bin/validate-env.sh`: doctor→init→plan); `bin/remove-env.sh` (+ wizard
`--remove`) tears down + removes an env. Defunct legacy envs deleted (52 files);
`.sops.yaml` reset to bare. ADR-0035 + plan `docs/plans/DMF Env Lifecycle Refactor Plan 2026-06-01.md`.

**dmf-env commits (pushed to private Forgejo, `648dca7..a833b8b`):** `c359355` resolver → `a9e66b6` generic TF root →
`6a49d85` wizard keys+operator-local → `ce6faa7` privkey materialize → `533ec60`
validate/remove → `5a53e1c` delete-defunct+helpers. Umbrella: ADR-0035 + plan committed.

**NOT yet live-verified** — the model is structurally validated (tofu validate,
bash -n, key round-trip) but no fresh env has been stood up on it. Next: operator
runs `bin/init-wizard.sh hetzner` (generate-key) → should reach a saved `plan.bin`
with **dmf-env staying clean** → `tf-apply <env> apply` → bootstrap → `--remove`.
**Cleanup nit:** stale untracked `terraform/{aliyun,aliyun-123,g2r6-foa9,hetzner-arm}/`
dirs (gitignored `.terraform/` + tfstate backups) — `rm -rf` them.

### 🟢 Upgrade-mechanism + CCM + teardown-script fixes LANDED (2026-06-01, UNPUSHED)

Implemented the prior handoff's scoped fixes (orchestrated; lifting via a Claude
worker pane, each commit reviewed). **6 commits on `main`, NOT pushed** — operator
to review/push. **No live cluster → structurally validated only** (except the k3s
tag, which exists).

**PUSHED to the private LAN Forgejo** after operator review.
- **dmf-env** `adabe88` tf-destroy LB-detach (network NAME→ID) + CCM default
  `v1.26.0`→**`v1.31.1`**; `e514bd9` unseal-openbao → node-local OpenBao HTTP API
  (use_pty-immune, UNTESTED); `4335d3c` `bundle_set` de-mask+instrument
  (`DMF_BUNDLE_SET_DEBUG=1`) + CWD-independent `.sops` + round-trip test;
  `d4306a7` new **`bin/upgrade-in-place.sh`**; `8fc7c05` review fixes (test sources
  real fns; wrapper `--dry-run`/exit-codes/k3s-caveat; unseal per-share re-resolve +
  curl precheck + `OPENBAO_API_ADDR` override preserved).
- **dmf-infra** `1b9976a` k3s→**`v1.33.12+k3s1`** (Path 3); `858fe74` 630 fail-fast
  assert on `vault_zot_service_password` (191 deliberately not guarded).

Operator decisions: **CCM Path 3** (hccm v1.31.1 + k3s 1.33); scope = all
code/script fixes not needing a live env. Review concern resolved: k3s is NOT
upgraded in-place by the wrapper (`300-k3s.yml` installs only when k3s absent →
no 1.30→1.33 minor-skip); the bump applies to **fresh builds only**.

**Still OPEN (live-env-gated):** confirm the real `bundle_set` failing command via
the new instrumentation; validate idempotency on a controlled `main~N → main` skew
env via `upgrade-in-place.sh`; validate the unseal rewrite live; extend
`verify-bootstrap-convergence.yml` (EE-in-Zot + catalog-JT-token — deferred).
Full detail + push commands:
[`docs/handoffs/DMF Upgrade-Mechanism + CCM + Teardown-Script Fixes Handoff 2026-06-01.md`](docs/handoffs/DMF%20Upgrade-Mechanism%20%2B%20CCM%20%2B%20Teardown-Script%20Fixes%20Handoff%202026-06-01.md).

### 🟢 g2r6-foa9 + Aliyun media nodes TORN DOWN (2026-06-01)

`g2r6-foa9` (Hetzner) and the two `aliyun-media-01/02` MXL-spike nodes are
**destroyed** (operator-decided wipe). Verified clean: no g2r6 servers / network
/ LB in `k3s-infra-lab`; Aliyun `tofu destroy` = 14 resources gone. Shared SSH
key `k3s-hetzner` **preserved** (it's `prevent_destroy`'d shared lab infra — must
survive env teardowns).

**Teardown gotchas hit (fix these in the scripts):**
- `hcloud_ssh_key.k3s` `prevent_destroy=true` aborts `tofu destroy` at plan. Must
  `tofu state rm module.cluster.hcloud_ssh_key.k3s` (with `-lock=false` — the
  JuiceFS backend's normal single-operator mode) to drop the shared key from the
  env's state before destroy.
- **`bin/tf-destroy.sh` LB-detach is BUGGY:** it compares the network *name*
  against the LB's network *ID* field, so it always says "not attached; skipping"
  and the private subnet/network destroy then hangs ~20min and times out. Manual
  fix: `hcloud --context k3s-infra-lab load-balancer detach-from-network
  g2r6-foa9-traefik --network g2r6-foa9-private`, then re-run destroy. (The CCM
  LB is data-sourced in TF — detach, don't delete, until destroy completes.)

**Rebuild prerequisite (MANDATORY before any new Hetzner env):** bump
`hcloud_ccm_version` ≥ `v1.30.1` in `dmf-env/tasks/hetzner/ccm.yml:15` — v1.26.0
crashes after **30 Jun 2026**. Version path (1/2/3) deferred to rebuild time. See
[`docs/plans/DMF Hetzner CCM Upgrade Plan 2026-06-01.md`].

**Upgrade-in-place mechanism still BLOCKED** (the original goal; g2r6 just exposed
it). Fix before validating on a controlled older-`main`→`main` skew env:
[`docs/plans/DMF Idempotent Upgrade-in-Place Mechanism — Findings and Plan 2026-06-01.md`](docs/plans/DMF%20Idempotent%20Upgrade-in-Place%20Mechanism%20%E2%80%94%20Findings%20and%20Plan%202026-06-01.md).
Keystone: `seed-bao` `bundle_set` fails on the cloud lane (exit 1, masked by
`| tee`); `seed-bao` is NOT auto-invoked by the bootstrap plays; root cause
UNPROVEN (failure precedes the sops-encrypt step). Also: `unseal-openbao.sh`
broken on hardened envs (`Defaults use_pty` drops piped stdin — re-run
`100-openbao.yml` instead). OpenBao correctly NOT ingress-exposed — keep it that way.

### 🟡 Authentik brand-wide passkey-first — landed on `main`, live verification PENDING (2026-05-29)

Extended passkey-first from the Console alone to the whole Authentik brand
(operator-chosen scope, ADR-0015 amendment). **Committed, not yet verified on a
live cluster.** dmf-infra `ae58ab8`: 6 non-Console providers
(Forgejo/AWX-SAML/NetBox/LibreNMS/Grafana/Zot) now pin `dmf-passkey-login`; the
default brand (`domain: authentik-default`) is patched to
`flow_authentication: dmf-passkey-login`. Console unchanged. Break-glass survives
at `/if/flow/default-authentication-flow/`. Cold bootstrap unaffected (enrollment
runs on its own `require_unauthenticated` flow). Plan +  ADR amendment in umbrella.

**Open:** human passkey gate (portal + an app land on the picker, no username
form) needs a cold bootstrap on the recreated `y834-bcwe` VM (deleted+recreated
empty today) or a fresh env id — dovetails with the §4.1 fresh-clone gate.
**Rollback gotcha (in plan):** the brand-patch lever does NOT auto-revert on
template removal (blueprints don't GC) — needs an explicit reset entry.

### 🟢 Sandbox gate #2 — catalog path OPERATOR-CONFIRMED end-to-end on imc1-cyh4 (2026-05-29)

Operator completed the passkey browser-Console pass: enrolled passkeys, logged in,
and drove deploy/teardown through the Console → AWX. The full
**human → browser → Console → AWX → nmos-cpp deploy/teardown** path is confirmed
working, attributed to `dmf-cms-svc`. Combined with the machine-path proof, the
`sandbox-single-node` gate is functionally met on imc1-cyh4; the remaining item
before a v0.1 tag is a **formal fresh-clone-by-a-non-maintainer** replicability
run (this was maintainer-driven).

**Follow-up logged (not built): deploy↔finalise race.** Catalog deploy
(`media-launch`, JT14) and finalise (`media-finalise`, JT15) are *different* job
templates, so the 0.9.2 same-action dedup guard does NOT serialize them. Firing
them ~1s apart raced on imc1-cyh4 (jobs 92+96): finalise ran before the deploy's
Helm release existed → no-op (`changed=0`) → deploy won → pods up + tag `active`
with a "successful" finalise that tore down nothing (Catalog correctly showed
`active`). Resolution: re-run finalise when nothing is racing. Fix direction:
per-catalog-entry cross-action serialization / lifecycle-aware lock. Sibling of
the sub-second same-action TOCTOU. See memory `project_dmf_catalog_deploy_finalise_race`.

### 🟢 Catalog double-launch guard — dmf-cms 0.9.2 (2026-05-29)

Operator double-clicked teardown (AWX jobs 76+80, 2s apart) — the Console button
re-enabled the instant the sub-second `/launch` POST returned while the AWX job
was still running. Deploy had the same latent gap.

Shipped in **dmf-cms 0.9.2** (commits `c9deb90` feature + `2464c0b` release;
`ghcr.io/dmfdeploy/dmf-cms:0.9.2` digest
`sha256:ba780f3a116ccec1207e11a6dda5bccb9e4cd893c87c94abfc8d167fa2fd4d6b`;
630-mirrored + 650-deployed on imc1-cyh4):
- **Backend (real guard):** `api_catalog_deploy/teardown` call new
  `awx.find_active_job_for_template` before launching; an in-flight job
  (new/pending/waiting/running) for the target template is returned idempotently
  (`status: already-active`) instead of a duplicate. Still via the dmf-cms-svc
  token (FIX 1 intact).
- **Frontend (UX):** buttons gate on the in-flight job id, not just the
  sub-second launch mutation; in-progress label.

Verified against live AWX (deploy JT14→job92 + teardown JT15→job96): the exact
dedup query returns the in-flight job → a 2nd request gets already-active, no
duplicate; created_by=dmf-cms-svc on both. Literal browser double-click needs a
passkey session (operator pass). Known out-of-scope gap: sub-second find→launch
TOCTOU isn't locked (reported clicks were 2s apart; frontend disable closes the
single-session case) — follow-up if DB-level locking is wanted.

### 🟢 AWX scoped-token identity (FIX 1) + offline catalog loop (FIX 2) landed on imc1-cyh4 (2026-05-29)

Two live-diagnosed fixes on the fresh sandbox env `imc1-cyh4`, both verified offline.

**FIX 1 — machine tokens minted AS the scoped svc identity (dmf-infra `64cd035`).**
AWX binds token ownership to the *authenticating* principal (not the URL user-id
or `description`), so both token paths — authed as admin — produced
break-glass-owned tokens; the Console's catalog launches were attributed to the
break-glass admin. Fix: gen+PATCH+persist a per-svc password and mint authed AS
the svc user (697 for dmf-cms-svc; awx-integration role for awx-svc), persisting
via `bao kv patch` (MERGE — never whole-secret PUT, the 69x sibling-wipe class).
Least-priv RBAC: JobTemplate Execute on the catalog launchers + Inventory Use on
NetBox. Verified: `created_by=dmf-cms-svc`; both svc identities self-own their
tokens; break-glass owns ZERO; `secret/apps/awx/runtime` retains all siblings.
Empirical ownership rule codified by orchestrator in ADR-0028 §C3.1.

**FIX 2 — offline catalog loop, EE-bake (dmf-infra `d2800a9` + `38d2db6`).**
The Console launch failed at project_update/inventory_update doing
`ansible-galaxy install netbox.netbox` against public galaxy.ansible.com (breaks
the self-contained sandbox, ADR-0030/0031). Fix: bake `netbox.netbox` (3.23.0) +
`pynetbox` (7.7.0) into the DMF AWX EE; neutralize the project's pushed
`collections/requirements.yml` to `collections: []` (do NOT rely on the Org
Galaxy-cred knob); pin the DMF EE on the NetBox **inventory source** (not just
the catalog JTs — inventory_update was still using the default EE → "unknown
plugin nb_inventory"). EE bumped to **0.1.1** and published:
- `ghcr.io/dmfdeploy/awx-ee:0.1.1` digest `sha256:867faa97ed7391befe599ca0ad96fab1f6dfeeddb25b542c79229b6434ee9b51`
  (built via colima docker-build, mirrored to cluster Zot by 630).
Verified offline on imc1-cyh4: media-launch → project_update + inventory_update
+ deploy all successful with **zero galaxy egress**, driven by the dmf-cms-svc
token; nmos-cpp registry+2 nodes Running; media-finalise teardown clean.
Permanent internal-collection-source = ADR-0034 (Forgejo-git), separate WP.
Footgun flagged: 630's EE-tag fallback is decoupled from the role default
(must hand-sync) — candidate for a single source of truth.


### 🟢 WP1S Phase 3 (AWX single-node fit) + Phase 4 (catalog loop) PROVEN on fresh env `imc1-cyh4` (2026-05-29)

Full cold rollout on a fresh Lima VM + wizard env (`imc1-cyh4`, operator
`marty-mcfly` / `delorean.dmf.test`, throwaway test identity). Prior env
`zy9q-1015` torn down; `wobe-9n0c` kept as reference.

**Catalog loop PASS** (driven via AWX API — Console browser path needs operator
passkeys, see below): `media-launch-nmos-cpp` (job 9, 38.6s) → health (IS-04
query API shows both mock nodes registered) → `media-finalise-nmos-cpp` (job 14)
→ idempotent re-deploy (job 19) → final teardown (job 24). All successful; `nmos`
ns clean at end.

**AWX single-node fit (WP1S Phase 3) — landed + tuned from observation:**
- `dmf-infra 0cc2cd6` — MAX_FORKS + postgres sizing + lean requests, gated on
  `dmf_awx_profile == single-node-low-concurrency`. (A1 disable-backup-CronJob =
  no-op, sandbox already skips it; A3 SCHEDULE_MAX_JOBS/CONCURRENT aren't real
  AWX settings — real cap is InstanceGroup `max_concurrent_jobs`, deferred.)
- `dmf-infra 3b2b6db` — first rollout OOMKilled `awx-web` at a 1Gi limit (exit
  137; 5 uWSGI workers + daphne need ~1.2Gi). Raised limits to lab-parity (web
  1536Mi, task 2Gi), kept lean requests. The "adjust once" per Phase C.

**Gate #8 (resource sanity) — PASS.** Footprint for WP5: awx-web 1178Mi,
awx-task 1040Mi (limits 1536/2048); catalog EE pod peak **192 MiB**
(cgroup-sampled; `dmf-catalog-cg` Container Group, ns `nmos`, currently
UNBOUNDED — rode through fine because actual is small). Node 8.6Gi/9.7Gi (76%),
~1.1Gi free at clean slate; never <939Mi free during jobs; **0 OOMKills** during
the loop. Escape hatch if a heavier item redlines: bound the EE pod via the
`dmf-catalog-cg` pod_spec_override (flagged, not built).

**sops cold-bootstrap bug fixed — `dmf-env 3ab4e50`.** On a fresh new-layout
sandbox, `seed-bao` exited non-zero at its final metadata-writeback with "no
matching creation rules found" (after all OpenBao seeding succeeded). Same sops
`--config` class as the wizard fix, recurring in `bootstrap-secrets.sh`'s two
re-encrypt paths: needed `--config` to the sibling per-env `.sops.yaml` AND the
temp file named = bundle basename (so it matches the creation_rule in both
lanes) + guarded `mv` + bash-3.2-safe. See [[project_sandbox_sops_config_class_bug]].

**`recreate-sandbox-vm.sh` — `dmf-env b3dffcd`.** Was hardwired to `wobe-9n0c` /
no-wizard reuse; now env-agnostic + VM-first→emit-IP for the fresh-wizard cold
path (legacy reuse kept behind `ENV_NAME=`).

**Open (operator, optional ~2 min — NOT a blocker):** ADR-0028 D8 verify gate
needs ≥2 confirmed operator passkeys; `marty-mcfly` has 0 (WebAuthn enrollment
is a human browser ceremony, not automatable). Everything else in
`bootstrap-sandbox-verify.yml` passed (incl. OIDC admin bridge: 7 apps, DMF
groups scope). Env `imc1-cyh4` left running at a clean slate for the operator to
enroll passkeys + drive the Console loop (gate #2) if desired. Teardown:
`rm -rf ~/.dmfdeploy/envs/imc1-cyh4` + `limactl delete -f dmf-sandbox`.

### 🟢 Break-glass-email hijack class fix landed (ADR-0024 §4) — `zy9q-1015` live-remediated (2026-05-28 close)

Mid-audit on the passkey enrollment story, operator surfaced a deeper
architectural concern: the wizard-defined operator user (`<operator-user>` on
`zy9q-1015`) is meant to be admin/superuser in every OIDC-backed app
via the `ops-admin` group projection (ADR-0024 §2, ADR-0028 C1), but
NetBox was showing `Username = netbox-break-glass`, `Full Name = <operator-user>`,
`Email = <handle>@gmail.com`, `Superuser = ✓` after the first OIDC
sign-in. **Diagnosis:** `secret/apps/<app>/admin.email` was being
seeded with the operator's real email for every non-Authentik app, and
the apps' social_auth pipelines (NetBox's `associate_by_email`,
Forgejo's `account_linking=auto`, AWX's default SAML pipeline) merged
the operator's first OIDC sign-in INTO the pre-existing break-glass
User row — username stayed `<app>-break-glass` but the profile,
social_auth link, and role bindings became the operator's. Same shape
of bug as the AWX shadow-superuser issue ADR-0028 lines 66-70
catalogued, via the email field instead of the username.

**Fix landed across all three repos:**
- **dmf-env `a039aa7`** — `seed-bao` writes the canonical pattern
  `<app>-<dmf_env_id>@<base-domain>` to `secret/apps/<app>/admin.email`
  for every non-Authentik app at first seed AND auto-heals legacy
  values (operator email, empty string, `admin@example.com`) on
  re-seed. Authentik keeps its `akadmin@<base-domain>` per the
  sanctioned exception.
- **dmf-infra `33a84e4`** — netbox / awx / forgejo role defaults now
  derive the same synthetic pattern from `dmf_env_id +
  cert_manager_cluster_domain`; the previous fallback to
  `vault_bootstrap_admin_email` is removed (reintroducing it requires
  re-amending ADR-0024 §4).
- **umbrella `64fdbb1`** — ADR-0024 §4 amendment codifies the pattern,
  the collision vector (social_auth pipelines + email as merge key
  with file pointers), and live evidence from the `zy9q-1015` hijack.

**Live-state remediation on `zy9q-1015`:**
- OpenBao `secret/apps/{forgejo,netbox,grafana,awx,zot}/admin.email`
  migrated to canonical via re-seed (auto-heal log shows each transition).
- NetBox: `netbox-break-glass.email` patched to
  `netbox-zy9q-1015@e2e.dmf.test` via `manage.py shell`; the hijacked
  `UserSocialAuth(user=netbox-break-glass, provider='oidc', uid='<operator-user>')`
  row deleted so `<operator-user>`'s next OIDC sign-in creates a fresh `<operator-user>`
  user instead of re-merging.
- Forgejo: `forgejo-break-glass.email` patched via REST API PATCH
  (`/api/v1/admin/users/forgejo-break-glass`).
- AWX: `awx-break-glass.email` patched via `awx-manage shell` (was
  `test@example.com`, not the operator's email — AWX was lucky).
- Grafana: `admin@localhost` already (sanctioned exception, no
  collision); `<operator-user>` already created as id=2 with the correct
  `GrafanaAdmin` role via `role_attribute_path` group check.
- Zot: htpasswd, no email field, no change needed.

**Operator action remaining:** sign in to NetBox + Forgejo via OIDC
to verify a fresh `<operator-user>` user is created with superuser by the
`set_role` / OAuth2 admin-group pipeline. Then proceed to passkey #2
via the DMF Console Settings page per
`docs/runbooks/passkey-enrollment.md` (R1 from earlier today).

### 🟡 R3 / R4 / R5 outstanding — pickup next session

Survey doc
[`docs/reviews/DMF Passkey Invitation Policy Alignment Survey 2026-05-28.md`](docs/reviews/DMF%20Passkey%20Invitation%20Policy%20Alignment%20Survey%202026-05-28.md)
§"Outstanding follow-ups" carries the full context. Quick reference:

- **R3** — Rename `/api/admin/invitations` →
  `/api/me/passkeys/invitation` + add D6 attestation fields
  (actor / role / request_id / reason) to the Console request log on
  the invitation-mint path. Best landed with R4.
- **R4** — Rebind Console's Authentik API token from `akadmin`
  (superuser) to a scoped `dmf-cms-svc` service account. **Attempted
  2026-05-28, pivot needed**: Authentik's User model returns `None`
  for `user_permissions` on service-account users; Authentik uses its
  own `authentik.rbac.models.Role` model (fields: `groups`, `users`,
  `initialpermissions`, `roleobjectpermission`,
  `rolemodelpermission`). Re-implement against the Role API, not
  Django Permissions. Token-creation flow was correct; only the
  permission-assignment step needs rewriting. ADR-0028 C3 violation
  while deferred — acceptable in experiment phase per ADR-0004,
  BLOCKING for ADR-0020 Mode B promote.
- **R5** — Platform-wide ADR-0007 §2 hardening (itoken-in-stdout
  posture across script + role debug + Console response + ntfy push).
  Should be one co-ordinated cross-repo PR, not piecewise.

### 🟢 Passkey enrollment self-heal landed on sandbox env `zy9q-1015` (2026-05-28)

Operator hit "Invalid invite/invite not found" on the bootstrap passkey URL.
Root cause: `bin/get-passkey-enrollment-url.sh` returned the OpenBao-cached
URL, but the live invitation in Authentik DB was gone (single-use consumed
or wiped — `TOTAL_INVITATIONS=0`, while user `<operator-user>` existed). The cache had
no way to know.

Fix shipped (Plan A v2 from the design pass):

- **dmf-infra `0722b8b`** — refactor: extract the inline mint Python into
  `roles/stack/operator/authentik/files/ak_passkey_invitation.py` (single
  source of truth, sourced via `lookup('file', …)` by both the role and the
  operator-helper); extract the OpenBao session block into
  `tasks/openbao_session.yml`; extract the invitation/parse/debug/ntfy/
  persist block into `tasks/ensure_invitation.yml`. main.yml shrinks
  870→625 lines via two `include_tasks` calls. Adds new mini-playbook
  `playbooks/vertical-security/111-authentik-passkey-ensure.yml` —
  the sanctioned entry point for re-minting just the invitation without
  re-running the full 110-authentik.yml. ADR-0010 compliant (state mutation
  flows through bin/run-playbook.sh).
- **dmf-env `69c6075`** — `bin/get-passkey-enrollment-url.sh` now detects
  when the cached `itoken` is missing from the live Authentik DB and
  self-heals by invoking the mini-playbook via `bin/run-playbook.sh`, then
  re-reads the freshly-cached URL. Single live probe returns both
  webauthn_count (existing) and invitation liveness (new). Adds
  `--read-only` to opt out of self-heal. `$DMF_INFRA_REPO` overrides the
  `../dmf-infra` sibling-checkout default.

Verified on `zy9q-1015`:

| Case | Behavior |
|---|---|
| Live invitation present | Fast path, ~4s, no mint |
| Live invitation present, `--read-only` | Same fast path, no side effects |
| Invitation consumed, `--read-only` | Clean error, no side effects |
| Invitation consumed, normal | Self-heal mints fresh URL via mini-playbook, ~22s |

ADR-0007 §2 trade-off (itoken in stdout) is **unchanged** — pre-existing
posture inherited from the role's debug+ntfy+OpenBao-plaintext path. Not
made worse by this change; full §2 hardening deferred to a separate
follow-up. ADR-0028 D8 minimum (≥2 confirmed devices) still enforced both
in the role and via the script's live count check.

**Plan B deferred:**
[`docs/plans/DMF Authentik Bootstrap Enrollment Drop Username Prompt Plan 2026-05-28.md`](docs/plans/DMF%20Authentik%20Bootstrap%20Enrollment%20Drop%20Username%20Prompt%20Plan%202026-05-28.md)
captures the follow-up to drop the username prompt from the enrollment
flow (option 2: swap identification stage for `user_write` in
`never_create` mode reading the invitation's `fixed_data`). Land after the
current self-heal path is verified end-to-end across both passkey
enrollments on `zy9q-1015`.

### 🧹 Sandbox env `9y6o-zn0t` torn down — ready for fresh e2e rollout (2026-05-28)

Operator brief: "fresh end to end sandbox rollout, pls teardown and remove the
current sandbox env completely including the artifacts." Done.

Wiped (mirrors yesterday's `cht6-781p` / `e7qm-gxig` purge pattern, plus the
Lima-VM host-side state which the cloud purges didn't need):
- **Workstation artifacts:**
  - `dmf-env/inventories/9y6o-zn0t/` — removed
  - `dmf-env/manifests/9y6o-zn0t.yaml` — removed
  - `dmf-env/.sops.yaml` 9y6o-zn0t rule — reverted (was uncommitted working-tree addition; now matches HEAD)
  - `$DMF_BOOTSTRAP_BUNDLE_DIR/9y6o-zn0t.sops.yaml` (encrypted bundle) — removed
  - `~/.secure/9y6o-zn0t/` (OpenBao breakglass JSON) — removed
- **Lima VM `dmf-sandbox` (<sandbox-node-ip>), substrate intact:**
  - `sudo /usr/local/bin/k3s-uninstall.sh` — k3s + containerd + /var/lib/{rancher,kubelet} swept; binary gone
  - `/etc/rancher` — removed
  - `/etc/hosts` DMF local-CA block (8 host entries from `321-local-ca-trust`) — removed via the `blockinfile` marker
  - Disk reclaim 22→3 GiB used (54 GiB free of 59)
- Other DMF envs (`g2r6-foa9`, `wobe-9n0c`, retired cloud envs) untouched.
- macOS Keychain: nothing to clean — sandbox profile sets
  `openbao_breakglass_distribution_enabled: false`, so no keychain shares were
  ever written for this env (the pre-existing `openbao-breakglass-share-3` is
  the lab's, untouched).

**Ready for fresh rollout:** operator runs `dmf-env/bin/init-wizard.sh` → new
opaque-slug env. The wizard now lands (per recent commits): RWO PVC mode,
`dmf_monitoring_profile: minimal` + sandbox-fit Prom/Loki/Grafana sizing, and
the post-seed wrapper auto-deploys the full monitoring vertical. The prometheus
self-scrape fix (`c2f33ee`) is in too — the next env starts with all chart
targets up from the first run.

### 🟢 Prometheus self-scrape fixed on BOTH lanes (2026-05-28)

The chart-default `prometheus` self-scrape job 404'd because the role sets
`server.prefixURL: {{ prometheus_path }}` (→ `--web.route-prefix=/prometheus`),
which moves the internal /metrics endpoint to `/prometheus/metrics` — but the
chart-default scrape hits `localhost:9090/metrics`. Showed perpetually down on
the Targets page on every env. Helm replaces lists wholesale (no partial merge
for `scrape_configs`), so I vendored the chart-default 10-job `scrape_configs`
from a live `prometheus-25.8.0` render into a sibling Jinja include
(`roles/base/prometheus/templates/scrape_configs.yml.j2`) with ONE local change:
`metrics_path: "{{ prometheus_path }}/metrics"` on the `prometheus` job. The
other 9 jobs are byte-identical to chart-default.

- dmf-infra `c2f33ee`, pushed to LAN.
- Re-deployed on both lanes via the wrapper; configmap-reload sidecar picks up
  the new ConfigMap on its sync (~60s), or POST `/-/reload` to trigger immediately.
- **Sandbox (9y6o-zn0t):** `job=prometheus health=up scrapeUrl=…/prometheus/metrics`. Counts: **11/11 up, 0 down** (was 10 up, 1 down).
- **Cloud (g2r6-foa9):** same fix verified. Counts: **20/20 up, 0 down** (was 19 up, 1 down).
- Refresh `scrape_configs.yml.j2` when bumping the prometheus chart version.

### 🟢 Sandbox monitoring vertical IN + live-verified on 9y6o-zn0t (2026-05-28)

Operator brief: implement the complete monitoring stack on the sandbox env,
sized to fit, including host (Lima VM) CPU/RAM/disk. Driven by claude-bottom
(this pane) per the [scope-locked plan §9](docs/plans/DMF%20Sandbox%20Monitoring%20Vertical%20Integration%20Plan%202026-05-27.md);
`claude` (pane 2) orchestrated + verified along the way. Five workstreams
landed across three commits (+ STATUS), all on `main`, pushed to LAN Forgejo.

- **W1 — Grafana local-CA OIDC trust (dmf-infra `c02a468`).** Third instance
  of the gate-2 class (zot → netbox → grafana). `roles/base/grafana` gains
  `grafana_local_ca_*` defaults + a publish-CA-ConfigMap task (read
  `dmf-local-ca` from cert-manager ns, publish into monitoring ns) +
  values.yml.j2 changes: chart's `extraConfigmapMounts` to mount `ca.crt`
  with subPath (preserves Alpine `/etc/ssl/certs` dir) at
  `/etc/ssl/certs/dmf-local-ca.crt`, plus `env: SSL_CERT_FILE: …` as the
  image-agnostic primary (Alpine Go cert-dir scan ≠ Debian/Zot). Gated on
  `dmf_tls_mode=='local-ca'`. **Verified live:** `SSL_CERT_FILE` env set,
  CA mounted, no `CERTIFICATE_VERIFY_FAILED` in Grafana logs.
- **W2 + W3 — Profile gate + post-seed wiring (dmf-infra `0c6ec00`).** The
  inert `dmf_monitoring_profile` var becomes real via a `when:` on each
  monitoring `import_playbook` in `bootstrap-sandbox-provision-post-seed.yml`.
  Sandbox default `minimal` (set in `bootstrap-sandbox-profile.yml`) means
  `prom → loki → promtail → grafana` deploy unattended; grafana ordered last
  so its pre-provisioned datasources resolve. `full` adds librenms/snmp/netdisco
  later (unwired). `none` skips. A clean rebuild now brings monitoring up.
- **W4 — Sandbox-fit sizing (dmf-env `57d897e`).** `init-wizard.sh`
  `render_sandbox_inventory` gains a monitoring sizing block: Prom 5Gi/7d/4GB,
  Loki 5Gi/168h, Grafana 1Gi (role default, already fit),
  `prometheus_alertmanager_enabled: false` (sandbox has no ntfy/watchdog URLs;
  role asserts those when enabled). Same hygiene-class as `netbox_media_access_mode`
  (16bc35b) — rendered into the inventory so standalone reruns stay fit. Verified
  ~37 GiB free on 9y6o-zn0t before locking; 11 GiB monitoring PVCs is comfortable.
  Also patched the live `9y6o-zn0t/group_vars/all/main.yml` (untracked env dir,
  operator's call to commit) with the same sizing + the missing
  `netbox_media_access_mode: ReadWriteOnce` (closes the STATUS 2026-05-27 gap).
- **W5 — Live deploy on 9y6o-zn0t (verification gates per plan §6):**
  1. ✅ `monitoring` ns: grafana, loki-0, loki-canary (DS), loki-gateway,
     promtail (DS), prometheus-server, prometheus-kube-state-metrics,
     prometheus-prometheus-node-exporter (DS) — all 1/1 Running, 0 restarts.
     PVCs Bound: prometheus-server=5Gi, storage-loki-0=5Gi, grafana=1Gi (all
     local-path RWO).
  2. ✅ `grafana.deploy.dmf.test` → 302 `/login` (Grafana, not the catch-all
     landing page).
  3. ✅ OIDC: `/login/generic_oauth` → 302 `https://auth.deploy.dmf.test/…/authorize`
     with PKCE S256 + `redirect_uri=https%3A%2F%2Fgrafana.deploy.dmf.test%2Flogin%2Fgeneric_oauth`.
     **No `CERTIFICATE_VERIFY_FAILED`** in Grafana logs. Passkey click-through
     is the operator's to confirm — front-channel intact.
  4. ✅ Datasources GREEN: Prometheus `up` returned 11 active targets (apiservers,
     nodes/kubelet, cadvisor, kube-state-metrics, node-exporter on the host
     `<sandbox-node-ip>:9100`, multiple pods). Loki `/labels` returned 13 indexed
     labels = promtail shipping + Loki ingesting + querying all working.
  5. ✅ Host metrics covered by the chart's bundled DaemonSets — `node_memory_MemTotal_bytes`
     query returned data with the node-exporter labels (host CPU/RAM/disk).
- **Cosmetic note (pre-existing, not a sandbox issue):** prometheus self-scrape
  `job=prometheus localhost:9090` shows `down` — chart default points at
  `localhost:9090` but the server is running under the `/prometheus` subpath.
  Same on g2r6-foa9 reference. Doesn't affect other targets, Grafana, or
  datasource health.
- **Rebuild path (Gap D from plan):** wired into post-seed; provable on next
  clean rebuild. Not exercised this session (operator brief was "modify
  9y6o-zn0t in place"); the gate is structural (declarative `when:`).

### 🟢 Catalog NMOS deploy 403 + NetBox OIDC login 500 — both fixed & live-verified on 9y6o-zn0t (2026-05-27)

Operator reported the catalog page's NMOS deploy failing (AWX job #9) on the fresh
e2e env `9y6o-zn0t`, plus a NetBox Authentik login error. Diagnosed both live
(read-only) and conferred with claude-bottom via agent-bridge before fixing. Two
dmf-infra commits on `main`, **committed not pushed** (operator pushes to LAN Forgejo):
`17a54e0` (awx-integration) + `1bb3ee2` (netbox).

- **Issue 1 — catalog 403 (ADR-0032 wiring regression, `17a54e0`).** `awx_integration_netbox_catalog_token`
  was only set inside the OpenBao source-token-read block gated `when:
  awx_integration_netbox_token | length == 0`. On a clean env the awx inventory token
  is already populated, so the block was skipped and the media-* JTs got
  `vault_netbox_catalog_token: ""`. The launcher fell back to the read-only `awx-netbox`
  token (no `extras.tag`) → 403 at nmos-cpp provision "Fetch existing NetBox tags". The
  scoped `dmf-catalog-svc` writer existed all along — pure wiring bug. Fix: default the
  var, widen the read gates (catalog-empty AND breakglass present, preserving the legacy
  vault-file path), split the set_fact so the catalog token is always set and the awx
  token is never clobbered. **Verified:** re-ran 693 → JT carries the scoped token (200 on
  extras.tags), `media-launch-nmos-cpp` job 18 **successful** end-to-end (nmos registry+2
  nodes Running, NetBox service → `lifecycle:active`). It "passed" on wobe-9n0c only
  because the awx token there was empty.
- **Issue 2 — NetBox OIDC login 500 = WP1S gate-2 NetBox half (`1bb3ee2`).** `/oauth/login/oidc/`
  500'd: python-social-auth does server-side OIDC discovery against the Authentik public
  host (local-CA), and the pod didn't trust the CA → CERTIFICATE_VERIFY_FAILED. Decided
  (with claude-bottom) NOT to mirror the CMS per-endpoint back-channel split — social-auth
  derives all endpoints from one discovery URL and Authentik bakes the public host into the
  doc, so it'd need a custom backend subclass. Instead mirrored the **zot** CA-mount pattern:
  publish `dmf-local-ca` into the netbox ns, mount `ca.crt`, set `REQUESTS_CA_BUNDLE`/`SSL_CERT_FILE`
  **in the NGINX Unit app `environment`** (Unit does NOT inherit container env — verified).
  Gated on `dmf_tls_mode==local-ca`. **Verified:** pod discovery 200s, `/oauth/login/oidc/`
  → 302 to the public authorize URL (front-channel intact). Interactive passkey click-through
  is the operator's to confirm.
- **⚠️ Inventory hygiene gap (dmf-env, NOT yet fixed):** the `9y6o-zn0t` inventory carries
  most sandbox capability vars directly but **misses `netbox_media_access_mode: ReadWriteOnce`**
  (set only by `bootstrap-sandbox-profile.yml`). Running `610-netbox.yml` standalone (outside
  the sandbox wrappers) defaults to RWM → helm fails patching the bound RWO `netbox-media` PVC
  (immutable). Worked around with `-e netbox_media_access_mode=ReadWriteOnce`. Recommend adding
  it to the inventory group_vars so standalone app-playbook re-runs on sandbox envs don't trip.

### 🟡 ADR-0033 — Zot scoped machine-write service account `zot-svc` MERGED to main + verified (not yet deployed) (2026-05-27)

Follow-on from the playbook-630 on-node fix: the operator flagged that Zot's only
authenticated identity was the break-glass `admin` htpasswd user, used for every
machine write (630 seed + zot-mirror cron) — an ADR-0028 C3/C4 violation (machines
should use scoped service accounts; no routine op should need break-glass). Zot was
the lone app still conflating the two (NetBox `dmf-catalog-svc`, Forgejo `forgejo-svc`,
AWX tokens already separate). Decided + locked as **[ADR-0033](docs/decisions/0033-zot-scoped-machine-write-service-account.md)**
(sibling of ADR-0032); [plan](docs/plans/DMF%20Zot%20Machine-Identity%20Service%20Account%20Plan%202026-05-27.md).
Also bundles the **630→control-node** structural fix (Part A: `hosts: k3s_control[0]`,
not workstation `localhost` — the sandbox lane has no Tailscale, and 630 seeds awx-ee
itself so it can't run in the EE pod).

- **New `zot-svc`** htpasswd user, scoped `read,create,update` on `**` (NO delete, NO
  adminPolicy); `admin` demoted to sole `adminPolicy` user = dormant break-glass.
  Tightened the LIVE OIDC-off `config.json.j2` branch (was `defaultPolicy
  read,create,update,delete` for ANY authed user → anon read + zot-svc push + admin-only).
- **Implemented by the `claude` pane**, reviewed by this pane (claude-bottom). Branch
  `feature/adr-0033-zot-svc-machine-write` in BOTH repos, committed, **NOT pushed**:
  - dmf-infra `0e9e38e` (zot defaults/tasks/config.json.j2; 630 repoint admin→zot-svc +
    on-node; zot-mirror→`secret/apps/zot/service`; openbao eso-reader + app-admin-writer
    gain `secret/data/apps/+/service`).
  - dmf-env `e2763d9` (bootstrap-secrets.sh only): `apps.zot.service_password` is a new
    **bundle** field (NOT an OpenBao-only/admin-copy) — it must exist pre-seed because
    331-registry-zot bakes the htpasswd before OpenBao exists. Generated at `init`;
    export-vars surfaces `vault_zot_service_password`; seed-bao auto-heals pre-ADR-0033
    bundles (generate + `bundle_set` + write `secret/apps/zot/service`). seed-bao now
    **mutates the encrypted bundle** on legacy envs (one-time migration; uses the
    existing `bundle_set`/cmd_rotate helper).
- **Review verdict (this pane): clean.** Verified: pre-seed bake premise (331 in
  bootstrap-provision-pre-seed); seed-bao re-run is **stable** (no password rotation —
  reads existing bundle field, OpenBao write no-ops if `zot-svc` present); config.json.j2
  renders valid JSON + correct least-privilege on all 3 branches; grep gate CLEAN (630 +
  zot-mirror carry no admin refs; remaining `zot_admin_*`/`apps/zot/admin` are all
  break-glass/adminPolicy/verifier/191/audit); ADR-0007 stdin-pattern parity with the
  admin loop; `bash -n` + `--syntax-check` pass.
- **MERGED + pushed to `main`** (2026-05-27): umbrella `08537c3` (ADR/plan/INDEX/STATUS),
  dmf-infra `0e9e38e`, dmf-env `e2763d9` + shellcheck fix `3796a38` + **init-wizard fix
  `7a7504b`**. FF merges; feature branches retained; untracked ADR portfolio review left intact.
- **Live e2e exposed a real gap → fixed (`7a7504b`):** the fresh env `e7qm-gxig` failed
  pre-seed/331 with `vault_zot_service_password | mandatory` undefined. Root cause: ADR-0033
  added the field to `bootstrap-secrets.sh cmd_init`, but env creation actually runs through
  `init-wizard.sh render_bundle`, which was NOT patched — so wizard'd bundles lacked
  `apps.zot.service_password`. (Miss in the original review: cmd_init was assumed to be the
  creation path; the wizard is.) Wizard now generates it alongside the other 32-char secrets.
- **Sandbox test envs PURGED for a clean e2e** (operator-confirmed): cht6-781p + e7qm-gxig
  fully removed (inventories, manifests, `.sops.yaml` rules, SOPS bundles, OpenBao keys).
  wobe-9n0c, cloud envs, and the Lima VM `dmf-sandbox` left intact.
- **e2e #2 on fresh env `9y6o-zn0t` got to post-seed ok=228, exposed a 2nd workstation→ingress
  bug → fixed (dmf-infra `5c4d04a`):** the cms role's "Assert dmf-cms image present in Zot"
  HEAD check was `delegate_to: localhost`, so it ran on the Mac, which can't resolve
  `registry.<domain>` on the no-Tailscale sandbox lane (`urlopen: nodename nor servname`).
  Dropped the delegate so it runs on the control node (same fix class as 630). Swept the
  rest of post-seed + all 6 verify sub-playbooks — every other ingress call already runs on
  `k3s_control[0]`; the 2 remaining cms localhost tasks (catalog find, lock release) are
  genuine local ops. This was the only remaining sandbox-lane delegate-to-localhost bug.
- **Verifiers made ADR-0033-consistent:** (1) `audit-admin-identities.yml` (`c3c4856`) —
  asserted Zot htpasswd == single `admin`; now expects exactly `{admin, zot-svc}` via an
  order-independent set match (still catches a missing zot-svc + unexpected extra users);
  this was the only verify-stage *failure*. (2) `verify-bootstrap-convergence.yml` (`b175470`)
  — added a `zot_service` `openbao_secret_present` record confirming `secret/apps/zot/service`
  is in OpenBao custody (username=zot-svc), alongside the existing `zot_admin` break-glass
  check. Together they realise the ADR-0033 Enforcement note (verify confirms zot-svc exists
  + is custodied). e2e #2 now expected to complete through `bootstrap-sandbox-verify`.
- **shellcheck DONE** via `uvx --from shellcheck-py shellcheck` (colima down; no local
  binary). Found + fixed one latent **SC1070** the implementer + `bash -n` both missed:
  the export-vars `python3 -c "…"` block is a *double*-quoted shell string, so the
  ` `| mandatory` ` backticks in the ADR-0033 comment ran a shell command-substitution at
  runtime → `bash: command substitution: syntax error near unexpected token |` on every
  `export-vars`/`run-playbook.sh` (non-fatal, python still ran, but noisy). Fixed in
  dmf-env **`3796a38`**. Remaining findings are pre-existing/intentional (the `sh -c '…'`
  SC2016 stdin pattern; a pre-existing SC2027 `""${domain}""` in `cmd_set_base_domain:2010`,
  unrelated to ADR-0033 — flag for separate cleanup).
- **OPEN for operator:**
  1. **Pending: clean e2e on a fresh wizard env.** With `7a7504b` a freshly-wizard'd bundle
     carries `apps.zot.service_password`, so the normal flow works unmodified: pre-seed (331
     bakes htpasswd with admin + zot-svc) → `seed-bao` (writes `secret/apps/zot/service`) →
     post-seed (630 + 140-zot-mirror auth as zot-svc). This is the not-yet-completed proof.
  2. **Legacy-bundle caveat (any pre-`7a7504b` bundle):** a full bootstrap CANNOT complete
     as-is — pre-seed/331 needs `vault_zot_service_password` (`| mandatory`) but a legacy
     bundle lacks it, and `seed-bao` (which would heal it) runs *after* pre-seed and needs
     the OpenBao pre-seed stands up. NOT fixable by "run seed-bao first". Resolve by re-init
     or by injecting `apps.zot.service_password` into the bundle (`sops --set`) before bootstrap.
  3. Minor cosmetic: zot role's reachability msg (`tasks/main.yml:389`) + zot-mirror README
     still name `admin`; harmless, defer.

### 🟢 ADR-0032 — catalog NetBox scoped writer DEPLOYED + verified on wobe-9n0c (2026-05-27)

Catalog Deploy (`media-launch-nmos-cpp`) was failing 403 and the console showed
"unknown". Root cause: read-only NetBox svc accounts lacked `extras.tag`, and the
launcher used the **superuser admin token** to write — the ADR-0028 C3 violation
the operator flagged. Fix per [ADR-0032](docs/decisions/0032-catalog-launcher-scoped-netbox-writer.md)
(qwen adversarial review, codex was out of quota):
[plan](docs/plans/DMF%20Catalog%20NetBox%20Scoped%20Writer%20Plan%202026-05-27.md).

- **Scoped writer `dmf-catalog-svc`** (group `dmf-catalog-writer`): `ipam.service`
  [view,add,change] + `extras.tag` [view,add] + `dcim.device` [view]. No superuser,
  no dcim write. Console `dmf-cms-readonly` gained `extras.tag` view (kills "unknown").
- **Shipped:** dmf-infra `db6dbcc` (netbox-sot writer+token+console perm; awx-integration
  wires `vault_netbox_catalog_token`, admin token dropped from catalog JTs) +
  dmf-runbooks `bf332bb` (nmos-cpp role: single scoped token, admin token gone, all 3
  stages). ADR/plan in umbrella `8aa95b9`. Implemented by the paired claude pane,
  reviewed by claude-bottom.
- **LIVE-VERIFIED:** AWX Deploy job 63 successful; nmos registry+2 nodes 1/1 Running;
  IS-04 Query API 200; NetBox `nmos-cpp` service tagged `lifecycle:active` via the
  scoped token; console catalog status = `active`. Apply chain: 691-netbox-sot →
  693-awx-integration → in-cluster Forgejo mirror-sync (in-pod) → AWX `update_on_launch`.
- **FOLLOW-UPS — all closed 2026-05-27 (#2 LibreNMS left per operator):**
  - **#1 691 token idempotency — FIXED + verified (generalized to ALL netbox-sot tokens).**
    dmf-infra `e340dc5` (claude pane): 691 now mint-if-absent for admin/awx/librenms/catalog
    — reuses the OpenBao-persisted token when a valid NetBox token still exists (NetBox can't
    re-derive the secret), gated on the OpenBao persistence path; vault-file path untouched.
    The persist `bao kv put`→`patch` (put only on true first-run create) so persisting one
    token no longer wipes siblings. Proven: re-ran 691 → "REUSED ... (no rotation)" for all
    four tokens; NetBox token identities unchanged (admin pk26, awx pk27, catalog pk25). A
    standalone 691 re-run is now a true no-op — no more breaking the AWX inventory credential,
    catalog JT, or runtime secret. (The earlier catalog-only fix was `57bc661`.)
    - **Caused + diagnosed live this session:** my repeated 691 re-runs (verifying the
      catalog-only fix) rotated the awx inventory token → AWX NetBox inventory-source
      credential went stale → inventory sync 401 → catalog TEARDOWN cascaded to fail.
      Unblocked by re-running 693 (re-wires the awx token); teardown then succeeded (job 77,
      nmos workloads removed, service flipped to `lifecycle:bootstrapped`).
    - **Residual (benign):** the pre-fix `kv put` already wiped `cms_api_token` from
      `secret/apps/netbox/runtime` (only admin/awx/catalog keys remain). Console is
      unaffected (it reads `dmf-cms-runtime/netboxApiToken` K8s Secret, intact). The
      put→patch now prevents recurrence, but restoring the OpenBao copy needs a forced 698
      (its gate keys on the K8s Secret, so a plain re-run skips). Low priority in a sandbox.
  - **#3a `add_user_to_group` 405 — FIXED.** dmf-cms `651593f`: PATCH the full member
    list instead of POST/`add_users` (+ regression test). Deploys with next dmf-cms release.
  - **#3b placeholder `operator` Authentik user — PRUNED.** Deleted (204, no groups,
    confirmed orphaned/no AWX dep); removes the live 405 noise. Users now: akadmin,
    break-glass, <operator-user>.
  - **`kv put`→`patch` (sibling-key wipe) — FIXED** as part of #1 (`e340dc5`).
- LibreNMS confirmed already out of the install chain (140-librenms commented in
  post-seed; netbox-sot gated `enabled:false`); residual verify-playbook refs are
  hygiene-only, deferred.

### 🟢 WP1S gate #2 — CMS Authentik front/back-channel split DEPLOYED + live-verified on wobe-9n0c (2026-05-27)

Login on `wobe-9n0c` 500'd and a new dmf-cms pod was CrashLoopBackOff. Root cause
(confirmed via live traceback): the dmf-cms pod doesn't trust the local CA, so every
server-side HTTPS call to `https://auth.boxsand.dmf.test` hit
`CERTIFICATE_VERIFY_FAILED` — startup group-bootstrap (Authentik mgmt API) crashed
the pod; `/auth/login` (OIDC discovery) 500'd. This was the documented WP1S gate #2.

**Decision (operator + codex cross-check): do NOT mount the CA into the pod.** Brought
Authentik into ADR-0023 compliance like awx/netbox/forgejo (already internal svc DNS)
via an explicit front/back-channel split. Plan + pattern:
[plan](docs/plans/DMF%20Console%20Authentik%20Internal%20Back-Channel%20Split%20Plan%202026-05-27.md).

- **Back-channel** (OIDC discovery/token/userinfo + Authentik mgmt API): internal
  svc DNS, plain HTTP. **Front-channel** (browser authorize redirect, callback
  redirect_uri, passkey enrollment URL): public host. Public issuer stays canonical.
- **Shipped 0.9.1** — dmf-cms `16dfd91` (tag `v0.9.1`) + dmf-infra `539f789`, both on
  `main`, pushed to forgejo-<operator>. Image `ghcr.io/dmfdeploy/dmf-cms:0.9.1` → mirrored to
  Zot (630) → deployed (650). 7 new tests green; codex diff review PASS (no P1).
- **LIVE-VERIFIED on wobe-9n0c:** pod `dmf-cms-5cf8df699c-*` 1/1 Running, 0 restarts,
  image `registry.boxsand.dmf.test/dmf-cms:0.9.1`; **no `CERTIFICATE_VERIFY_FAILED`**,
  "Application startup complete"; `/auth/login` via Traefik → **302** to
  `https://auth.boxsand.dmf.test/.../authorize` with `redirect_uri=https://console.boxsand.dmf.test/auth/callback`
  + PKCE S256. The interactive passkey click-through is the operator's to confirm.
- **FOLLOW-UP (non-blocking):** startup `add_user_to_group` (bootstrap user →
  `dmf-console-admin`) gets Authentik **405 "POST not allowed"** — a pre-existing
  `authentik.py` bug (wrong method; should be PATCH on the group or the users endpoint)
  now *surfaced* because the call finally reaches Authentik. Logged-and-continued (the
  0.9.1 `OSError`/`AuthentikAPIError` catch keeps startup from crashing). Fix the method
  in a later patch. In OIDC mode the real operator gets admin via Authentik group
  membership anyway, so this isn't on the login path.
- The build was `--dirty` (the other Claude's uncommitted `AdminOverview.tsx` enrollment
  UI rode along in the 0.9.1 image, per operator "ship everything").

### 🟢 WP1S sandbox lane — local-CA TLS + single-Traefik collapse codified & proven (2026-05-26)

The Tailscale-disabled single node had no coherent in-cluster OIDC path
(Forgejo→Authentik over `https://auth.<base-domain>` failed: orphaned
traefik-private Ingresses, no local CA, no app cert). Operator chose **collapse
to the bundled k3s Traefik**; codex-reviewed and implemented:

- **dmf-infra `32fa17b`** — cert-manager local-CA branch (`dmf_tls_mode==local-ca`):
  selfsigned→CA→`dmf-local-ca` ClusterIssuer → apex+wildcard default cert →
  Traefik default TLSStore; new **`321-local-ca-trust.yml`** (reproducible
  coredns-custom explicit host mappings + node `/etc/hosts` + node CA trust,
  CA read via `k8s_info`); Forgejo CA-trust mount; ingress-class collapse in the
  sandbox prelude. Cloud/lab defaults untouched.
- **dmf-env `9b691a3`** — `external_base_url`→`https://<base-domain>`,
  ingress classes→`traefik` (wizard + wobe-9n0c inventory), manifest
  `public_lane/private_lane`→single `local_lane`.

**Proven on wobe-9n0c (codified path, no live hacks):** 321 reruns clean
(in-cluster `auth.<domain>` resolves, node trusts CA); `620-forgejo` reaches
Ready with external `https` verify (`validate_certs:true`) passing,
`configure-gitea` exit 0. Codex verdict: accepted for WP1S proof progression.

**Remaining gates (NOT blockers for this step):** (1) clean VM rebuild + full
sandbox bootstrap = the definitive from-scratch proof of the `320` local-CA
branch (it was *adopted* on this cluster, not recreated); (2) NetBox/CMS/AWX
server-side Authentik HTTPS trust (same CA-trust pattern, runtime OIDC).

### 🟡 WP1S sandbox lane — items 1 & 2 landed (2026-05-26)

Picked up codex's in-flight WP1S handoff. Two dmf-infra commits, in the
operator's requested order (nmos patch first, then substrate):

- **`b644c3a`** — nmos-cpp seed-chart fallback in `630-zot-seed-platform.yml`
  realigned `0.1.0`/`sha256:4de2…` → `0.1.1`/`sha256:827bce…` (matches the
  live chart). The OSS v0.1 version-manifest gap (both `.md` and `.yaml`) is
  downgraded **release-blocker → patched**; the residual cross-source
  agreement check is now WP5's obligation, not knowingly-wrong tree state.
- **`b171b8d`** — sandbox-single-node bootstrap substrate: `*-profile` capability
  prelude + four `bootstrap-sandbox-*` wrappers, profile-conditional
  `--disable=servicelb`/`--disable=local-storage` in `300-k3s.yml`, and a new
  `single-node-servicelb` ingress mode. Lab wrappers untouched; all syntax-checked.

**Item 3 — local sandbox VM + first-acceptance: DONE (2026-05-26).** First
acceptance PASSED: k3s Ready on the node's bridged LAN IP, Traefik serving
80/443 via ServiceLB, default local-path PVC binds.

- Backing host is a **Lima** VM `dmf-sandbox` (Debian 12 bookworm, aarch64,
  4 CPU / 10 GiB / 60 GiB), **not Colima** — colima 0.10 is hardwired to its
  Ubuntu image and can't boot Debian (`-i` validates against colima's own
  hash); Lima `template://debian-12` (vz) does. Debian chosen to match the
  Hetzner target so the Layer-2 host playbooks behave identically.
- **Bridged networking** (socket_vmnet on en0): the node has a real LAN IP
  (`<lan-ip>`, DHCP) and a single routable identity; the Lima user-mode
  NIC stays as the management lifeline but the cluster never references it.
  We investigated bridged-only (drop the user-mode NIC) and rejected it: it's
  unsupported in Lima and buys nothing, since the lane already targets the
  routable IP VPS-style.
- Reachability decision: bridged **+** Tailscale (both). Tailscale deferred —
  it'll be added the platform way during the bootstrap sequence, not ad-hoc.
- New inventory `dmf-env/inventories/dmf-sandbox` (`a243604`) — provider-
  agnostic, VPS-style SSH to the routable IP. The same lane runs on bare
  metal / VPS / QEMU; only ansible_host / k3s_node_ip / k3s_private_interface
  differ. hosts.ini + local_secrets.yml (throwaway k3s_token pre-OpenBao)
  stay local per .gitignore.

**Item 4 started — verifier conditioned (DONE 2026-05-26):** `301-k3s-verify`
(`roles/base/post-bootstrap-verify`) was an entirely *cross-node* verifier
(asserted ≥2 ready nodes, pinned client/echo to distinct nodes) and so could
never pass single-node, blocking the sandbox verify path. Made capability-aware
in dmf-infra **`16c302a`**: `≥1`-node gate; single-node mode colocates the
client+echo and still proves CNI/CoreDNS/Service/ingress/LB, forgoing only the
cross-NODE guarantee. The `≥2` multi-node path is unchanged. Verified passing on
the live single-node sandbox.

**Codex BOM review (2026-05-26):** addressed in umbrella `ac4e416` (+ codex's
status-only `e10e721`): de-staled the nmos-cpp manifest note and added a
machine-readable `host_os` section (sandbox = Debian 12 bookworm linux/arm64,
gating; AWS = Debian 12 arm64 AMI, secondary) with a WP5 host-OS fail-condition.

**End-to-end bring-up = a conditioning chain (in progress).** Running the
sandbox wrappers end-to-end surfaced that the bootstrap is a chain of
cloud/lab assumptions to condition for single-node-local, one link at a time:

- **OpenBao secrets bootstrap — conditioned (DONE 2026-05-26).** The decided
  sandbox unseal posture (searched + confirmed: ADR-0031 O4 / Profile 1, WP0
  §Secrets boot, WP1S, WP2 re-scope banner, tiered-unseal framework §5.3
  "self-recovering") is **simple local seed/unseal, no operator-Mac / JuiceFS
  / Keychain / USB**. Implemented in dmf-infra **`70452a4`**: a
  `openbao_breakglass_distribution_enabled` gate (default true → lab unchanged)
  that, when false, skips all USB/JuiceFS/Keychain distribution + asserts and
  keeps only the local automation file; unseal guarded on key count for 1-of-1.
  Sandbox vars in dmf-env **`80dab21`** (operator-local custody form chosen;
  keys under `~/.secure/dmf-sandbox`, 1/1 Shamir). Posture-guard assert + note
  fix added in **`6a16a43`**. **LIVE-VERIFIED 2026-05-26:** `320-cert-manager`
  then `100-openbao` → bao initialized 1-of-1, `Sealed false`, raft/HA, keys at
  `~/.secure/dmf-sandbox/openbao-keys.json` (0600), USB/JuiceFS/Keychain
  distribution skipped. (A pre-existing `openbao-breakglass-share-3` Keychain
  item is the LAB's, untouched by the sandbox run.)
- **cert-manager — works as-is for sandbox OpenBao TLS** (codex correction; my
  earlier "needs local-ca conditioning to skip ACME" was wrong). `320-cert-manager`
  skips the ACME/Cloudflare/default-cert/TLSStore path cleanly when
  `cert_manager_acme_email` is unset, and the internal OpenBao self-signed
  Issuer creates `openbao-tls`. **External app TLS for `*.dmf.test` (Traefik
  TLSStore / default cert) remains a separate later concern**, only to be
  patched if a live run proves it the next blocker — not pre-emptively.
- **Seed-material tooling — partial.** `bootstrap-secrets.sh` now supports a
  `sandbox`/`local` provider (dmf-env **`05e68fa`**): validator accepts
  `metadata.provider` in {sandbox,local,none} + skips cloud-token checks; `init`
  detects sandbox from env name, writes `provider`, skips CF/Tailscale/notif
  imports. A throwaway `dmf-sandbox` bundle was created + **doctor-validated
  (12/12)** — proving the patch. `.sops.yaml` got a `dmf-sandbox` rule
  (committed). **This dmf-sandbox env is transitional and will be retired** (see
  env-model decision below).
- **Env-model decision (2026-05-26):** sandbox envs get **cloud-parity identity**
  — an auto opaque `xxxx-xxxx` `dmf_env_id` (like cloud `g2r6-foa9`) used as the
  inventory dir + bundle + env_id, **plus** a user-set **subdomain label** →
  `base_domain = <label>.dmf.test`. `dmf_provider: sandbox`. The Lima VM
  `dmf-sandbox` is the *host* (separate from env_id). `dmfadmin` (bootstrap_admin)
  is confirmed = the sanctioned local **break-glass** admin (ADR-0028: Authentik
  is steady-state; break-glass is dormant/bootstrap-only; name must be
  collision-free with OIDC — `dmfadmin` is).
- **init-wizard `sandbox` provider — DONE (WP1S Phase 1).** Built by the paired
  claude pane, reviewed here + by codex (3 review rounds). dmf-env **`4b2305a`**
  (provider: opaque slug → dir/bundle/env_id; subdomain → `<label>.dmf.test`;
  arm64-forced; skips all cloud machinery; writes single-node hosts.ini +
  sandbox profile group_vars + bundle + sops rule + manifest; cloud paths
  byte-for-byte preserved). Codex blockers fixed: **`a9960ad`** (doctor skips
  object-storage for sandbox/local/none; `eso_openbao_breakglass_file` wired in
  the render + transitional inventory) and **`84b38ed`** (`seed-bao`
  `acquire_temp_root` now threshold-aware — reads `shamir_threshold` from the
  break-glass JSON, default 3 legacy; sandbox 1-of-1 works, lab 3-of-5 preserved;
  verified across 4 fixture cases). `bootstrap-secrets.sh` sandbox provider:
  **`05e68fa`**.
- **NEXT — run the wizard → real slug env, then the chain.** Operator runs
  `init-wizard.sh` (sandbox; subdomain label; node SSH <sandbox-node-ip>/<operator>/
  ~/.lima/_config/user; interface lima0) → opaque-slug env. Then: retire the
  transitional `dmf-sandbox` (inventory/bundle/sops-rule/`~/.secure/dmf-sandbox`),
  reset the VM's OpenBao (likely recreate the Lima VM for a clean substrate),
  and run the full chain under the slug: **pre-seed → seed-bao → export-vars →
  post-seed → configure → verify** (Authentik + apps). AWX fit (item 5) after.
- **TRACKED FOLLOW-UP — strip operator-local values from the remaining `bin/`
  scripts.** Wizard + bootstrap-secrets are clean (`056d16c`), but 6 other
  dmf-env `bin/` scripts hardcode `/Volumes/<operator>/secure/...` (fallback defaults in
  `bootstrap-operator-approle.sh`, `get-admin-cred.sh`, `rotate-approle-secret-id.sh`,
  `unseal-openbao.sh`; hardcoded in `generate-awx-control-node-keypair.sh`,
  `tf-apply.sh`) and `<lan-host>` (`export-openbao-vars.sh` comments). Fix:
  introduce a `DMF_SECURE_DIR` config var (sourced from `~/.config/dmf/env` like
  `DMF_BOOTSTRAP_BUNDLE_DIR`), replace the hardcoded secure paths, genericize the
  comments. Deferred 2026-05-26 per operator (do after the live bring-up).

dmf-infra `6a16a43`, dmf-env `056d16c`, umbrella pushed to `forgejo-<operator>`.

### ✅ ADR-0028 Identity and Authority Chain — landed (2026-05-24)

Three-reviewer chain (operator + claude-bottom + codex + claude-top)
landed the full identity & authority architecture in one session:

- **[Architecture doc v1.2](docs/architecture/DMF%20Identity%20and%20Authority%20Model.md)** —
  5 contract statements (C1-C5), 8 decisions (D1-D8), 3 refinements
  (R1-R3), bootstrap/steady-state postures + convergence audit gate, 4
  security planes diagram, per-app matrix now populated from the survey.
- **[ADR-0028](docs/decisions/0028-identity-and-authority-chain.md)** —
  Accepted, binds the architecture, lists the three confirmed
  sanctioned exceptions (Authentik `akadmin`, Zot `admin`, Grafana
  `admin`). Supersedes ADR-0024 §Alternatives' "Generalise the helper
  to read OpenBao too" deferral; ADR-0024 entry amended in place.
- **[Per-App Identity Feasibility Survey](docs/plans/DMF%20Per-App%20Identity%20Feasibility%20Survey%202026-05-24.md)** —
  closed by claude-top (`dmfdeploy@2fbcef3`). No fourth exception
  emerged. AWX shadow superuser explained as username-collision
  artefact; fix landed as D3 follow-on (rename + SOCIAL_AUTH
  alignment), not a carve-out.
- **[Helper plan v3](docs/plans/DMF%20Unified%20App-Admin%20Helper%20Plan%202026-05-23.md)** —
  committed; reframed as implementation under ADR-0028 D1's OpenBao
  custody half. Eight-PR sequence still binding; framing now correct.
- **[OIDC client-secret rotation runbook](docs/runbooks/oidc-client-secret-rotation.md)** —
  new operator procedure. Triggers: incident response (ADR-0007 rule 6),
  scheduled rotation (D8), personnel change.

**Incident note (2026-05-24, experiment-phase risk acceptance per [ADR-0004](docs/decisions/0004-experiment-phase-stance.md)):**
during claude-top's `env|grep` probe of the live dmf-cms pod on
`g2r6-foa9`, `DMF_CONSOLE_OIDC_CLIENT_SECRET` surfaced to the agent
transcript. Operator decision: **defer rotation for this env only** —
`g2r6-foa9` is a short-lived test env, not Mode-B / Mode-C / real-user.
Risk closes at env teardown. Runbook documents the procedure for the
case where deferral is not available. For any future env serving real
users, rotation is binding per the runbook.

**Follow-on work status (not blocking; track separately):**
- **Done 2026-05-24:** AWX local-admin rename +
  `SOCIAL_AUTH_PIPELINE` alignment (`dmf-infra@c426dc0`).
- **Done 2026-05-24:** NetBox + Forgejo local-admin rename to dedicated
  break-glass identities (`dmf-infra@3953c62`). **Grafana evaluated and
  intentionally not renamed 2026-05-25**: ADR-0028's sanctioned
  exception for Grafana `admin` stays in force because the login form is
  disabled, OIDC auto-login is the routine path, and the chart-default
  local admin is dormant break-glass only.
- **Done 2026-05-24:** D2 bootstrap-convergence verifier playbook
  (`dmf-infra@0f9e7f0`) with live PASS on `g2r6-foa9`.
- **Mostly done 2026-05-24/25:** D8 operational hardening implementation
  landed (`dmf-infra@2dcafb9`) and the enrollment helper was aligned in
  `dmf-env@4ae3971`. Live adoption on `g2r6-foa9` is with Claude:
  apply Authentik TTLs, mint the next passkey invitation, enroll the
  second passkey, then run the D8 verifier.
- **Postponed pending further review:** ADR-0029 draft — *Tiered Unseal
  Posture for OSS Deployment*. Design captured in
  [DMF Unseal Posture Tiered Framework Plan 2026-05-24](docs/plans/DMF%20Unseal%20Posture%20Tiered%20Framework%20Plan%202026-05-24.md).
  Reframes ADR-0011's auto-unseal trade as explicit Tier 1/2/3 chosen
  at deploy time (Quorum / Operator / Self-recovering). Includes HA bao
  + raft baseline, cluster-resident unseal kiosk, passkey + PRF + QR
  cross-device share submission. Air-gapped/flypack variant deferred to
  follow-on plan. Operator decision 2026-05-25: postpone ADR drafting
  and HA/raft implementation until a further review pass.
- ~~Helper plan v3 implementation (8 PRs); now binding under ADR-0028 D1.~~
  **PR 1a / 1b / 2 / 3 landed 2026-05-24** (`dmf-infra` commits
  `30dfad5`, `52b261d`, `bc6699b` + `2e13cb3`, `f74533b`; main,
  pushed). PR 4 (STATUS sweep + ADR-0024 amendment) and PR 5
  (`g2r6-foa9` `bootstrap-verify.yml` rerun) are also complete.

### ✅ Unified App-Admin Helper Plan v3 — PRs 1a / 1b / 2 / 3 landed (2026-05-24)

Implementation of [ADR-0028 D1](docs/decisions/0028-identity-and-authority-chain.md)
OpenBao custody half, per the [helper plan v3](docs/plans/DMF%20Unified%20App-Admin%20Helper%20Plan%202026-05-23.md).
Codex coordinated; claude-bottom (this pane) drafted; codex reviewed
between each PR. All commits are on `dmf-infra/main` and pushed.

- **PR 1a** (`30dfad5`) — `common/app-admin-facts` delegates OpenBao
  session establishment to `common/openbao-session`. Existing
  materialize consumers (110-authentik × 2, 191-zot-oidc) updated to
  include the session role immediately before the helper. 191
  post-task validation block (~40 lines of bespoke breakglass JSON
  load + inline `bao auth/userpass/login` + sed token extraction)
  collapses to one `bao kv get` task. Behaviour byte-identical;
  ~70 lines net removed.
- **PR 1b** (`52b261d`) — new `app_admin_mode: live-read`. Resolution
  is **strictly source-atomic** via a dict-lookup keyed on the
  resolved source: each layer must supply BOTH username and password
  or the layer is rejected. Order: OpenBao → paired
  `app_admin_fallback_candidates` → scalar fallback → no-fallback.
  Diagnostic `<prefix>_source` fact. Fixture-test playbook at
  `k3s-lab-bootstrap/tests/test-app-admin-facts-live-read.yml` (six
  scenarios) duplicates the resolution Jinja verbatim and asserts no
  cross-layer mixing.
- **PR 2** (`bc6699b` + corrective `2e13cb3`) — 698 Forgejo half
  migrated onto live-read. Bespoke ~50-line OpenBao read +
  fallback-chain set_fact replaced by one `include_role`. Fallback
  candidate 2 matches the forgejo-bootstrap role-default vault
  pairing: `vault_bootstrap_admin_username` +
  `(vault_forgejo_admin_password | default(vault_bootstrap_admin_password, true))`.
  Source-atomic. NetBox half untouched (correctly out of scope —
  runtime/data secrets, not admin identity).
- **PR 3** (`f74533b`) — `roles/stack/operator/forgejo-bootstrap`
  admin reads migrated onto live-read. Role defaults
  `forgejo_admin_username` / `_password` reduced to empty strings;
  `dmfadmin` and `| mandatory` literals dropped (replaced by the
  helper's no-fallback path + a hard post-resolution assert). 15
  downstream `url_username` / `url_password` references unchanged
  via set_fact rebinding from helper output. Service-credential
  `openssl rand -base64 24` flow explicitly preserved per plan.

**Behaviour change envelope (intentional, scoped to admin-identity
resolution):** the pre-helper inline patterns silently permitted
cross-layer username/password mixing (e.g. username from
`vault_bootstrap_admin_username` + password from
`forgejo_admin_password`). Post-PR-3, the helper rejects any layer
where either field is empty; empty resolution fails loud at the
consumer's assert. Greenfield envs with proper vault inventory
(either explicit `forgejo_admin_*`, or `vault_bootstrap_admin_*`
with at least one of `vault_forgejo_admin_password` /
`vault_bootstrap_admin_password`) continue to work. Envs that
relied on cross-layer mixing must supply paired credentials.

**Verification status:**
- `ansible-playbook --syntax-check` clean on all touched playbooks
  (`110-authentik.yml`, `191-zot-oidc.yml`, `698-cms-netbox-forgejo-tokens.yml`,
  `692-forgejo-bootstrap.yml`).
- Fixture test passes (ok=36, failed=0).
- **PR 5 live verification on g2r6-foa9: PASS (2026-05-24).**
  - `bootstrap-verify.yml`: `ok=102 changed=3 failed=0 skipped=5`.
    All four imported plays clean
    (`verify-openbao-identity-model`, `audit-admin-identities`,
    `verify-oidc-admin-bridge`, `vertical-resilience/190-resilience-verify`).
    audit-admin-identities reported: "all assertions passed.
    apps audited: 5 (awx, forgejo, netbox, zot, authentik)".
  - `191-zot-oidc.yml` rerun: `ok=48 changed=4 failed=0 skipped=10`.
    Refactored post-task block emits the persistence-confirmation
    flow as before — five tasks: `Validate Zot admin secret was
    persisted to OpenBao` (ok), `Assert Zot admin secret is readable`
    (ok, "All assertions passed"), `Roll Zot StatefulSet` (changed),
    `Wait for Zot StatefulSet to be ready after restart` (ok after
    expected retries), `Verify Zot OIDC discovery endpoint is
    reachable` (ok). PR 1a session-fact-based refactor is
    behaviour-equivalent.
  - Logs at `/tmp/dmf-playbook-logs/pr5-bootstrap-verify-20260524-153444.log`
    and `/tmp/dmf-playbook-logs/pr5-191-zot-oidc-20260524-154350.log`
    on the operator workstation.
- **Extra direct consumer reruns: PASS (2026-05-24).** `110-authentik.yml`
  `ok=74 changed=3 failed=0`; `692-forgejo-bootstrap.yml`
  `ok=65 changed=1 failed=0`; `698-cms-netbox-forgejo-tokens.yml`
  `ok=25 changed=0 failed=0`. `698` verified the intended no-op path
  because both dmf-cms runtime token keys already existed; forcing its
  helper branch would require deliberate token-key deletion.
- ADR-0024 §Alternatives + §Enforcement amended in place (PR 4 of
  the plan) to record the landed helpers, the two modes, and the
  K8s-Secret/OpenBao backend split.

**Three-reviewer pattern note:** drafter + adversarial reviewer +
independent code-grounded reviewer caught material the two-Claude
pattern missed (NetBox-OpenBao-admin misclassification, Shamir
distribution wording, attribution-deferral wording). Recommend
repeating for any architecture-scale work going forward.

### Current live test env

**`g2r6-foa9`** — Hetzner CAX21 ARM64, nbg1, cluster domain
`<lan-host>`. Substitute this into every `<env-name>` placeholder
in the skills + canonical docs. Three Tailscale nodes at `100.64.0.22-24`;
SSH key `~/.ssh/id_ed25519_k3s_hetzner` with `ansible_user=k3s-admin`.
Retired envs still present in `dmf-env/inventories/`: `hetzner-arm`,
`aliyun`, `aliyun-123` (history; do not push to).

### ✅ dmf-cms v0.9.0 landed in g2r6-foa9 (2026-05-23)

The May 12 commits (`cb3aef3` backend module, `328f857` live React /catalog
+ Deploy/Teardown UI, `1b4c259`/`0addc19` ConfigMap plumbing, `a959778`
in-cluster DNS) were stuck behind v0.8.0 (cut 2026-05-11). v0.9.0 cuts +
ships them. Live cluster now running `registry.<lan-host>/dmf-cms:0.9.0`;
served JS bundle `index-g2WATqgD.js` contains `Deploy`, `Teardown`,
`useDeployCatalog`, `useTeardownCatalog` — original regression fixed.

Release artefacts:
- Forgejo: `dmf-cms@8f9ba75` + tag `v0.9.0`
- GHCR: `ghcr.io/dmfdeploy/dmf-cms:0.9.0` @ `sha256:006e4b2f...`
- Zot mirror: `registry.<lan-host>/dmf-cms:0.9.0`
- Deployment: `dmf-cms-659787d687-q6zx5` Running 1/1 on `g2r6-foa9-node-03`

### ✅ Build/release drift swept (2026-05-23)

Four drift items surfaced while driving v0.9.0; all fixed and committed:

1. **dmf-cms-build-and-release skill** previously said playbook 650 builds +
   pushes from the workstation (pre-ADR-0025). Rewritten end-to-end for the
   GHCR-canonical / Zot-mirror flow; `dmf-cms/docs/DEVELOPMENT-AND-BUILD-RULES.md`,
   `release.sh`, and `build-image.sh` aligned.
2. **`hetzner-arm` baked into durable docs.** Replaced with `<env-name>`
   placeholder in umbrella + dmf-env CLAUDE.md, the dmf-cms-build-and-release,
   dmf-cluster-access, and dmf-openbao-unseal skills, dmf-cms scripts, and
   IMPLEMENTATION-STRATEGY.md. Concrete env id lives only in STATUS.md.
3. **Playbook 630 hardcoded `cms_image_tag: '0.8.0'` default.** Now reads
   `dmf-cms/VERSION` via `lookup('file', …)`, matching the cms role default.
   Standalone re-mirror no longer needs `-e cms_image_tag=<X.Y.Z>`.
4. **`verify-cluster.sh` hardcoded `registry.dmf.example.com`.** Now accepts
   `DMF_CMS_REGISTRY` env var (default unchanged, public-prose placeholder).
   End-to-end-tested against the live cluster.

Bonus: removed the baked-in `DEFAULT_ENV="hetzner-arm"` in
`dmf-env/bin/run-playbook.sh`; the wrapper now requires `ENV_NAME` and lists
available inventories when missing. Bundle-dir split (workstation SOPS bundle
vs USB tfvars per the 2026-05-22 §D plan) surfaced in dmf-env CLAUDE.md.

### 📌 Bundle-dir decision — Option 2 for the experiment phase (2026-05-23)

`DMF_BOOTSTRAP_BUNDLE_DIR` reconfigured to `$HOME/secure/dmf-bootstrap` —
the workstation-disk path where the g2r6-foa9 wizard actually wrote. The
encrypted USB volume requirement is dropped for the duration of the
experiment phase per [ADR-0004](docs/decisions/0004-experiment-phase-stance.md);
FileVault remains the disk-layer guarantee. Decision and revisit gates
recorded in [`docs/plans/DMF g2r6-foa9 Configure-Verify Follow-Ups Plan 2026-05-22.md`](docs/plans/DMF%20g2r6-foa9%20Configure-Verify%20Follow-Ups%20Plan%202026-05-22.md) §D.

**Revisit when any one holds:**
- ADR-0020 Mode B implementation begins (managed-service trust story
  needs removable-media isolation) — or any third-party / paying-customer
  pilot uses this workstation as bootstrap origin.
- ADR-0004 experiment-phase stance is superseded or amended.
- Bundle contents grow to include material whose loss-by-disk-theft
  outcome is meaningfully worse than loss-by-USB-theft.

**Operator-side action (outside this repo):** edit `~/.config/dmf/env` to
`export DMF_BOOTSTRAP_BUNDLE_DIR=$HOME/secure/dmf-bootstrap`. Retired envs
(`aliyun/`, `aliyun-123/`, `hetzner-arm/`) on the USB volume stay where
they are; the discarded `z4ud-sy22` directory under `$HOME/secure/dmf-bootstrap/`
can be removed.

**Wizard hardening still wanted** (separate `dmf-env` commit, not
blocking): warn-or-fail in `init-wizard.sh` when a `/Volumes/*/dmf-bootstrap/`
exists but the resolved dir is under `$HOME` — surfaces the split posture
if the USB ever gets remounted before a wizard run.

### ✅ Lane B Chart 0.1.1 landed end-to-end on g2r6-foa9 (2026-05-23)

The chart-bump followup closed in one session. Chart `nmos-cpp` is now at
`0.1.1` with cluster-internal Zot DNS image-repo defaults; helm release
`nmos-cpp` in `nmos` upgraded from revision 1 (chart 0.1.0) to revision 2
(chart 0.1.1) in place. Workloads stayed up (image refs unchanged — only
chart packaging changed). All five Phase 6 verification checks green.

**Live state:**
- GHCR: `oci://ghcr.io/dmfdeploy/charts/nmos-cpp:0.1.1`
  digest `sha256:827bce1a0a769bd996033232d178ad1b057879f4619d8ef17ca1281903bd5ba0`
- In-cluster Zot: `registry.<lan-host>/dmf/charts/nmos-cpp:0.1.1`
  (mirrored by Stage 4b reseed)
- AWX project `dmf-runbooks` scm_revision: `d321d09` (launcher chart-version bump)
- AWX job 147 `media-launch-nmos-cpp`: successful in ~21s
- Helm release `nmos-cpp` rev 2, status `deployed`, chart `nmos-cpp-0.1.1`
- NetBox `lifecycle:active` tag set on the nmos-cpp service entry

**5-check Phase 6 verification:**
1. ✅ JT EE `DMF AWX EE` / `zot.zot.svc.cluster.local:5000/dmf/awx-ee:0.1.0`
2. ✅ `helm list -n nmos` shows `nmos-cpp` chart `nmos-cpp-0.1.1` rev 2 deployed
3. ✅ Resource count: 1 STS + 2 Deps + 3 Svcs + 4 CMs + 1 PVC (matches 0.1.0)
4. ✅ NetBox `lifecycle:active` flip confirmed in job 147 stdout
5. ✅ NMOS Query API HTTP 200 from in-cluster curl pod

**Step 9 closed (dmf-infra `258f940`):** awx-integration role default
bumped to `nmos_cpp_chart_version: "0.1.1"` + digest
`sha256:827bce1a…`. Verified live: re-run of 630-zot-seed-platform.yml
*without* `-e` overrides reports "0 chart(s) need seeding; 1 already
present and skipped." Standalone 630 runs now match the launcher's
hardcoded version without manual `-e` flags.

**DEFAULT_ENV cleanup closed (dmf-env `9c51136`):** seven scripts that
still defaulted ENV_NAME to the retired `hetzner-arm` inventory now
require it as the first positional arg and die with the env list +
STATUS.md pointer. Mirrors `f26294e`'s `run-playbook.sh` fix.
Smoke-tested both no-arg (lists envs, exits 1) and valid-arg paths.

**Item 3 deferred — AWX JT extra_vars exposure (lab-fine, prod-not):**
the AWX launch API returns the full JT `extra_vars` payload in the
launch-response body, including plaintext NetBox tokens stored on the
JT. Acceptable in the experiment phase; the migration to an AWX
Custom Credential Type is scoped + designed in
[`docs/plans/DMF AWX Catalog Credential Type Migration Plan 2026-05-23.md`](docs/plans/DMF%20AWX%20Catalog%20Credential%20Type%20Migration%20Plan%202026-05-23.md)
(~100 min of work, one role-task commit + live verification). Pick
up before any non-lab caller sees JT-launch responses.

### ✅ Lane B honorable-mentions hygiene swept (2026-05-23)

Three honorable-mentions items from the
[`DMF Lane B Hygiene Followups Handoff 2026-05-23.md`](docs/handoffs/DMF%20Lane%20B%20Hygiene%20Followups%20Handoff%202026-05-23.md)
landed across four commits — chart self-consistency, wizard hardening, and
retired-cluster-IP footgun all closed.

**dmf-env (`4c2be37` + `71ab061`, pushed):**
- `fix(bin): remove retired-cluster IP fallback in 4 SSH_TARGET defaults` —
  strips the literal `k3s-admin@<node-public-ip>` from `get-admin-cred.sh`,
  `bootstrap-operator-approle.sh`, `rotate-approle-secret-id.sh`, and
  `unseal-openbao.sh`. (Handoff §3 listed three; `unseal-openbao.sh`
  carried the identical pattern and is swept too.) Scripts now die loudly
  when neither env nor inventory yields an SSH target. Closes Lane B
  item #3.
- `fix(init-wizard): warn when bundle-dir is under $HOME but USB volume
  mounted` — adds the split-posture detection to `check_bundle_dir`.
  Warns + continues (experiment-phase posture per ADR-0004); the bundle-dir
  §D Option 2 decision made workstation-disk legitimate, so the wizard
  surfaces a still-mounted USB without blocking. Closes the §D wizard
  hardening item #2.

**dmf-media (`0318b3a`, pushed):**
- `fix(charts/nmos-cpp): default image repos to cluster-internal Zot DNS;
  bump 0.1.1` — chart 0.1.1 ships with
  `zot.zot.svc.cluster.local:5000/dmf/nmos-cpp-{registry,node}` as the
  baked-in image repos. helm lint clean; `helm template` renders the
  cluster-internal refs. Removes the standalone-consumer-pulls-from-example.com
  footgun.

**dmf-runbooks (`d321d09`, pushed to LAN + GitHub mirror confirmed):**
- `fix(launch-nmos-cpp): bump chart to 0.1.1; drop redundant image-repo
  overrides` — `nmos_cpp_chart_version: "0.1.0"` → `"0.1.1"`;
  `release_values:` cleaned of the now-redundant `registry.image.repository`
  + `node.image.repository` overrides. `createNamespace: false` retained
  (still load-bearing). Closes the launcher half of Lane B item #1.
- GitHub mirror confirmed: `0eb94d1..d321d09 main -> github/main` at fetch.

**Steps 4-8 of the chart-bump handoff remain operator-side (out of agent
scope per `dmf-cluster-access` §0 secrets discipline):**

1. **Operator publishes chart 0.1.1 to GHCR** (Step 4):
   ```bash
   cd "$DMFDEPLOY_UMBRELLA"
   security find-generic-password -s "ghcr.io" -a "<github-username>" -w \
     | GHCR_USER="<github-username>" \
       dmf-media/bin/publish-chart-to-ghcr.sh
   ```
   Then make the GitHub package public via the Packages UI.

2. **In-cluster Forgejo pull-mirror + AWX project sync** (Step 7) — needs
   Forgejo + AWX admin creds via `bin/get-admin-cred.sh g2r6-foa9 <app>`,
   run in a separate terminal so the secrets don't enter agent transcripts:
   ```bash
   # set fj_user/fj_pass + awx_user/awx_pass via stdin reads
   curl -fsSk -u "$fj_user:$fj_pass" -X POST \
     "https://forgejo.<lan-host>/api/v1/repos/forgejo-svc/dmf-runbooks/mirror-sync"

   proj_id=$(curl -fsSk -u "$awx_user:$awx_pass" \
     "https://awx.<lan-host>/api/v2/projects/?name=dmf-runbooks" \
     | jq -r '.results[0].id')
   curl -fsSk -u "$awx_user:$awx_pass" -X POST \
     "https://awx.<lan-host>/api/v2/projects/$proj_id/update/"
   ```

3. **Stage 4b reseed** (Step 8) — mirrors chart 0.1.1 from GHCR into
   in-cluster Zot:
   ```bash
   cd "$DMFDEPLOY_UMBRELLA/dmf-env"
   ANSIBLE_LOCAL_TEMP=/tmp/.ansible ANSIBLE_REMOTE_TEMP=/tmp/.ansible \
     DMF_BOOTSTRAP_BUNDLE_DIR="$HOME/secure/dmf-bootstrap" \
     ./bin/run-playbook.sh g2r6-foa9 \
     ../dmf-infra/k3s-lab-bootstrap/playbooks/630-zot-seed-platform.yml
   ```

4. **JT relaunch + Phase 6 verification gate** (Step 10) — `media-launch-nmos-cpp`
   via AWX API or dmf-cms catalog page; check `helm list -n nmos` shows
   `nmos-cpp` chart `0.1.1`.

**Step 9 (`nmos_cpp_chart_version` in awx-integration defaults) deferred** —
the role default currently couples version to digest
(`sha256:4de20135…` is the 0.1.0 digest, consumed by 630-zot-seed-platform.yml
as an integrity check). Bumping just the version would trip the digest
gate. Land version + new digest together in a single dmf-infra commit
after the operator publishes 0.1.1 to GHCR and the new digest is known.

**Other footguns surfaced while sweeping but not fixed** (separate scope —
flagged here so the next session can pick them up):
- `DEFAULT_ENV="hetzner-arm"` is still baked into
  `dmf-env/bin/get-admin-cred.sh:33` (and probably the sibling scripts).
  Same "stale literal default for a retired cluster" shape as the
  `7757eb2` + this session's IP fix; the `f26294e` run-playbook fix
  did not propagate to the per-script positional-arg defaults.
- `dmf-env/DEPLOYMENT.md` + `dmf-env/docs/hetzner-provisioning.md`
  reference `<node-public-ip>` in prose about the historical hetzner-arm
  env. Documentation drift, not a runtime footgun; sweep alongside
  the next dmf-env doc pass.

### ✅ dmf-runbooks public history remediated to v0.1.2 (2026-05-23)

Follow-up security/presentation audit found that public `v0.1.0`/`v0.1.1`
history made superseded private-node topology comments reachable, and that the
public per-repo guard files themselves contained operator-identity breadcrumbs.
`dmf-runbooks` was re-orphaned to `0eb94d1` (`v0.1.2 - public history
remediation`) and pushed to both LAN Forgejo and GitHub.

Current public refs:

```
main   -> 0eb94d1
v0.1.2 -> 0eb94d1
```

Deleted from GitHub + LAN Forgejo: public tags `v0.1.0`, `v0.1.1`, and stale
`master`. Local forensic tags remain workstation-only; do not push `--tags`.
GitHub branch protection was temporarily removed for the sanitized force-push
and restored afterward (linear history required, force-push/delete disabled,
admins included).

Verification from a fresh public clone:
`gitleaks --log-opts=main`, `gitleaks --log-opts=--all`, `gitleaks --no-git`,
and `bin/scrub-public-repos.sh dmf-runbooks` are clean. The umbrella scrub
gate was fixed in `bin/scrub-public-repos.sh` so empty context patterns no
longer crash under `set -u`, and `.gitleaks.toml` / `.github/CODEOWNERS` are
no longer allowlisted for identity matches.

### ✅ ADR-0025 Lane B landed (2026-05-23)

Catalog launcher migration complete. `media-launch-nmos-cpp` now runs
in-cluster via the AWX EE pod + Container Group, installs the NMOS-cpp Helm
chart from in-cluster Zot into `nmos`, and flips the NetBox lifecycle tag to
active. The hardcoded Hetzner private-IP `ansible_host` band-aid is removed.
ADR-0025 is Accepted; ADR-0016 is fully superseded for `media-*` JTs and
retained for 693-class infrastructure plays only.

Verified on `g2r6-foa9`: AWX job 131 succeeded at 2026-05-23T11:48Z; Helm
release `nmos-cpp` is deployed in `nmos` as chart `nmos-cpp-0.1.0` (revision
4); NMOS registry + two mock-node workloads are Ready and the Query API
returned HTTP 200 with mock nodes registered.

Operational finding captured in ADR-0025: containerd image pulls run in the
node host network namespace, so `*.svc.cluster.local` image refs require the
now-landed base/k3s fixes: pinned Zot Service ClusterIP, node-side `/etc/hosts`
entry, and containerd `certs.d/hosts.toml` plain-HTTP override.

### ✅ ADR-0024 Two-Identity Admin Model landed (2026-05-22)

The 2026-05-08 Pre-Bao Secrets Design's "one shared bootstrap admin
identity" is now formally codified. ADR-0024 (Accepted) captures the
two-identity model: a uniform local admin per app
(`vault_bootstrap_admin_username` + `vault_bootstrap_admin_password`) and
the same human enrolled in Authentik as an `ops-admin` group member for
SSO. Three sanctioned exceptions: Authentik (`akadmin`), Zot (`admin`),
Grafana (`admin`).

**What shipped:**
- ADR-0024 at `docs/decisions/0024-two-identity-admin-model.md`;
  INDEX.md updated from Reserved → Accepted.
- `dmf-infra/k3s-lab-bootstrap/roles/common/admin-identity-resolve/` —
  new helper role that reads admin user + password from a K8s Secret
  with caller-supplied fallback. ADR-0007 compliant (no_log, no argv).
- `awx-integration/tasks/main.yml` refactored: 28-line inline
  K8s-Secret read replaced with 12-line `include_role` of the helper;
  now resolves both username and password (was password only).
- `playbooks/697-cms-awx-token.yml` refactored: PR1's same-day narrow
  set_fact fix (`dmf-infra@142f819`) replaced with helper invocation.
  Same effective behaviour, single abstraction.
- `playbooks/verify-oidc-admin-bridge.yml` — assertion-only smoke test
  for the OIDC bridge (operator user in ops-admin, every app's
  provider + application provisioned, all OIDC providers carry the
  DMF groups scope). Tagged `vertical-identity`.

**What did NOT change scope (vs original handoff):**
- NetBox and Forgejo store their local admin in OpenBao
  (`secret/apps/<app>/admin` via `common/app-admin-facts`), not in a
  K8s Secret. The helper is K8s-Secret-specific and intentionally not
  adopted there; the existing OpenBao read patterns continue to work.
- `698-cms-netbox-forgejo-tokens.yml` keeps the narrow PR1 fix at
  `dmf-infra@4f7f505` (Forgejo username fallback chain) — same reason.
- Per-env opt-in flag plumbing for `aliyun-123` / `hetzner-arm`
  inventories was skipped: both clusters are torn down and retired;
  flags in retired inventories reconcile nothing.

**Retired clusters — followup cleanup:**
- `dmf-env/inventories/aliyun-123/`, `inventories/hetzner-arm/`, and
  `inventories/aliyun/` correspond to torn-down clusters. Many doc
  references remain (historical plans are fine to leave; CLAUDE.md and
  `.claude/skills/dmf-cluster-access/`, `dmf-openbao-unseal/` are
  forward-looking and should be re-pointed at `g2r6-foa9`). Separate
  cleanup task — not part of ADR-0024 work.

### ✅ dmf-runbooks Path A public publish landed (2026-05-22)

First DMF repo public on GitHub. `dmfdeploy/dmf-runbooks` is now the
canonical pull source for in-cluster Forgejos and the destination of LAN
Forgejo push-mirror auto-sync. Closes the 2026-05-21 handoff end-to-end.

**Pipeline now live:**

```
Operator -> LAN Forgejo (<lan-forgejo-host>/<handle>/dmf-runbooks)
                |
                | push-mirror with branch_filter=main, sync_on_commit
                v
            github.com/dmfdeploy/dmf-runbooks (public, branch-protected main)
                |
                | pull-mirror (forgejo_mirror_repos, 1h interval)
                v
            per-cluster in-cluster Forgejo (g2r6-foa9 live; future envs auto)
                |
                v
            AWX project sync -> catalog JT create
```

**Public state on GitHub:**
- `main` = single orphan commit `d19b6af` (v0.1.1, leak-scrubbed)
- Tags: `v0.1.0` (3e9f4f9, original 2026-05-07 orphan), `v0.1.1`
- No `archive/*` or `pre-republish-*` refs propagated (Gate 1 ✓)
- LICENSE Apache 2.0, NOTICE attributes Sony nmos-cpp, gitleaks clean
- Branch protection: linear history required, force-push + delete blocked,
  applies to admins
- README and roles/nmos-cpp/README polished: relative `../docs/` umbrella
  links replaced with plain ADR/plan-name references (no broken links for
  first-time viewers)

**LAN Forgejo state:**
- Push-mirror live; `branch_filter=main` prevents stale `master` from
  propagating
- Forensic tags `archive/pre-publish-2026-05-07` + `pre-republish-2026-05-21`
  deleted from LAN Forgejo to prevent leak-bearing history propagating via
  Forgejo's all-tags-default. They survive on the operator workstation only.

**dmf-infra role change (`a604812` on `main`, pushed):**
- `forgejo-bootstrap` role now uses `/repos/migrate` instead of PATCH for
  pull-mirrors. Forgejo invariant verified 2026-05-22 on Forgejo
  14.0.3+gitea-1.22.0: PATCH on existing non-mirror repo returns 200 but
  silently no-ops the mirror flag; only `/repos/migrate` actually wires
  the mirror subsystem. Includes new
  `tasks/ensure-mirror-repo.yml` with GET-check + DELETE-if-not-mirror +
  POST-migrate idempotency.
- 692-forgejo-bootstrap.yml on g2r6-foa9 verified idempotent (no
  destructive action against live mirror, mirror_updated unchanged).

**Bootstrap unblock signal:** AWX catalog JT create step in
`awx-integration/tasks/main.yml:1085` (the original blocker that
triggered this whole work stream) **succeeded** on g2r6-foa9's
bootstrap-configure run. The role-default + mirror combo closed the
loop. A separate downstream failure (`697-cms-awx-token.yml` AWX 401
on `dmf-cms-svc` lookup, likely AWX admin password drift) now gates
bootstrap-configure completion — out of Path A scope, separate
followup.

**Notes for future sessions:**
- LAN Forgejo at `<lan-ip>` (not `forgejo.<lan-host>`, which
  is NXDOMAIN) is the canonical operator push target. The handoff doc
  used "lab Forgejo on Hetzner" as the canonical target name; reality
  is the LAN one. Doc correction worth landing.
- `forgejo.<lan-host>` resolves to `100.64.0.22-24` =
  g2r6-foa9 in-cluster Forgejo (downstream consumer, not push target).
- The 5 remaining public repos (umbrella `dmfdeploy`, `dmf-cms`,
  `dmf-central`, `dmf-infra`, `dmf-media`) follow the same procedure but
  each needs its own pre-publish leak audit. Scrub already found 200+
  `<lan-host>` matches in the umbrella's docs/ tree and 8 in
  dmf-infra's k3s-lab-bootstrap/ that need cleanup before they can go.
- `dmf-env` never goes public (Gate 2 — contains inventory, IPs,
  OpenBao role_id, hcloud token refs).

### 🚧 NMOS catalog launcher pivot consolidated with runner-pod work — plan landed (2026-05-19)

The 2026-05-17 `media-launch-nmos-cpp` failure on aliyun-123 (AWX job 44,
`UNREACHABLE!` due to Hetzner-only hardcoded private-IP map in
`dmf-runbooks/playbooks/launch-nmos-cpp.yml`) triggered an architectural
pivot. The fix consolidates with the in-flight in-cluster Ansible runner
pod plan (2026-05-14, Phase 1 landed) into a single convergence: **Ansible
runs in in-cluster pods using a Zot-hosted EE image; catalog functions
deploy as Helm charts.** Plan + ADR placeholder are now in tree:

- Plan: [`docs/plans/DMF Cluster-Internal Ansible Execution and Catalog Helm Pivot Plan 2026-05-19.md`](docs/plans/DMF%20Cluster-Internal%20Ansible%20Execution%20and%20Catalog%20Helm%20Pivot%20Plan%202026-05-19.md)
- Public image registry implementation plan: [`docs/plans/DMF Public Container Registry Publishing Plan 2026-05-19.md`](docs/plans/DMF%20Public%20Container%20Registry%20Publishing%20Plan%202026-05-19.md) — recommends GHCR (`ghcr.io/dmfdeploy/*`) as public source of truth and Stage 4b mirroring into Zot for runtime pulls.
- ADR placeholder: [ADR-0025](docs/decisions/0025-ansible-in-cluster-pods-and-catalog-helm.md). (ADR-0024 remains reserved for App Admin drift per Aliyun-123 follow-ups §B.1.)
- ADR-0016 amended (partially superseded for `media-*` JTs). ADR-0012 + ADR-0023 clarifying notes added. Move-1-2026-05-06 Path A pivot plan carries a partial-superseded banner. Runner-pod 2026-05-14 plan is now Lane C of the converged plan. MXL spike 2026-05-17 cross-refs the unified Stage 4b seeding mechanism.
- Seven open decisions all **answered 2026-05-19 with recommended defaults** (see `docs/agentic/decisions-open.md` and the batch entry in `docs/agentic/autonomous-decisions.md`):
  - §8.1 Stage 4b seed: workstation Ansible play (`600-zot-seed.yml`)
  - §8.2 dmf-media build-and-release skill: defer
  - §8.3 ADR-0025 scope: keep broad
  - §8.4 EE build host: operator workstation (Colima)
  - §8.5 NMOS arm64 images: confirmed in operator's local Colima `docker-build` profile (path scrubbed per dmf-operator-identity rule; tag `:0.1.0`, built 2026-05-19)
  - §8.6 Zot pull policy: anonymous on `dmf/*`
  - §8.7 AWX pod placement: SA-in-target-ns + Container Group (option B)
- **Public registry plan landed:** `docs/plans/DMF Public Container Registry Publishing Plan 2026-05-19.md`. GHCR is canonical public source; Zot mirrors via Stage 4b.
- **Implementation kicked off:** NMOS Dockerfile hardening + GHCR push script landing in this session; canonical `:0.1.0` GHCR push happens after Dockerfile rebuild with pinned upstream SHA.

### ✅ Lane A milestone — all four DMF images on GHCR (2026-05-19)

Four canonical DMF-built images published to `ghcr.io/dmfdeploy/`:

| Image | Tag | Visibility | Digest |
|---|---|---|---|
| awx-ee | 0.1.0 | **Public** | `sha256:bdd802bb598df46714abdfa919e9b07491bfc85bfd3443111aee372bca93e63d` |
| dmf-cms | 0.8.0 | **Public** | `sha256:10ed8bd03651101cb7aa656d02dfd91acf32060512bc78373451352c8fed80db` |
| nmos-cpp-registry | 0.1.0 | **Public** | `sha256:da252712306fe876bd25a5b40f8ea1740eec46fe35e75a7c3acfb98950bbe622` |
| nmos-cpp-node | 0.1.0 | **Public** | `sha256:21c2a441f2f4c5577a269b54ec23e573c3206a6811c23fcf47621e54f51f124f` |

All four images: arm64/linux, **public**, anonymous pull verified from
empty `DOCKER_CONFIG`. First canonical DMF-built image set on the
public internet.

**Image provenance:**
- awx-ee built from `quay.io/ansible/awx-ee:24.6.1` + DMF additions
  (kubernetes.core, community.general, ansible.posix, community.docker
  + OCI labels). Source: dmf-infra@88cb81f at build time.
- dmf-cms built from the dmf-cms repo (image already existed locally;
  this session retagged + pushed). VERSION-asserted match per
  ADR-0005 / public registry plan §5.3.
- nmos-cpp-{registry,node} built from Sony nmos-cpp at upstream commit
  `8e2e17f` (Apr 7 2026 — RFC 6763 service-name fix). Pin baked into
  each image at `/etc/dmf/nmos_cpp_ref` and recorded in OCI label
  `org.dmfdeploy.upstream.nmos_cpp_ref`.

**Build pipeline** lives at `dmf-infra/k3s-lab-bootstrap/ee/` (AWX EE)
and `dmf-runbooks/roles/nmos-cpp/files/` (NMOS Dockerfiles). All three
per-repo `publish-to-ghcr.sh` wrappers delegate auth + push to the
umbrella's `bin/publish-image-to-ghcr.sh` (consolidated 2026-05-19).

Stage 4b mirror to in-cluster Zot waits for the new cluster
(`playbooks/630-awx-ee.yml` runs on first bootstrap-configure). ADR-0025
graduates from Proposed → Accepted at that point.

### ✅ aliyun-123 lifecycle-configure bootstrap completion — landed + live-verified (2026-05-17)

The four gaps surfaced in the
[2026-05-15 bootstrap-completion plan](docs/plans/DMF%20Lifecycle-Configure%20Bootstrap%20Completion%20Plan%202026-05-15.md)
all shipped and pass §6 verification against `aliyun-123`:

| Commit (`dmf-infra@main`) | Topic |
|---|---|
| `a891ecb` | feat(lifecycle-configure): seed repos, CMS perms, fail-loud JT (Gap A + B + C) |
| `440ec61` | fix(forgejo-bootstrap): seed via deny-list (`.git/`) — supersedes `f1ba770` |
| `8d20e71` | fix(awx-integration): trigger AWX project sync per catalog project |

Live cluster validation (run 2026-05-17T12:51, `failed=1` at unrelated
inventory gap — see follow-ups plan §A.1): Forgejo `dmf-runbooks` has the
launcher playbooks; AWX `dmf-runbooks` project synced to revision
`aea4a518…` with playbooks indexed; catalog JTs `media-launch-nmos-cpp` +
`media-finalise-nmos-cpp` created; `dmf-cms-svc` returns HTTP 200 on
`/api/dcim/sites/` and `/api/ipam/services/` (previously 403); dmf-cms
`/api/catalog` reachable, OIDC-gated as designed.

**All follow-ups consolidated** in
[DMF Aliyun-123 Lifecycle-Configure Follow-Ups Plan 2026-05-17](docs/plans/DMF%20Aliyun-123%20Lifecycle-Configure%20Follow-Ups%20Plan%202026-05-17.md):
five quick-win items (Class A — incl. the privkey inventory pin that
caused today's only failure), persistent app-admin drift decision-gate
(Class B.1 — ADR-0024 candidate: Path 3 patches vs fresh rollout),
strategic workstreams carried from 2026-05-14 (Class C — runner-pod
Phases 2–4, internal DNS migration), doc-vs-implementation drift items
(Class D — break-glass `root_token`, breakglass-hygiene SHA compare,
NetBox drf-spectacular), bonus observations (Class E). The plan
supersedes the 2026-05-15 plan as the active tracker.

### ✅ aliyun-123 bootstrap-configure — first clean end-to-end run (2026-05-14)

After 13 attempts and 11 distinct walls debugged, `bootstrap-configure.yml`
on `aliyun-123` reached `PLAY RECAP failed=0` end-to-end (run 13). Operator
passkey enrollment URL retrieved + enrolled. Full Authentik OIDC chain now
testable from a real browser session.

Session handoff:
[DMF Aliyun-123 Bootstrap Green-Run + ADR-0023 + Runner Spike Handoff 2026-05-14](docs/handoffs/DMF%20Aliyun-123%20Bootstrap%20Green-Run%20%2B%20ADR-0023%20%2B%20Runner%20Spike%20Handoff%202026-05-14.md)
— full per-wall debugging log + outstanding workstreams.

**Three docs landed this session** (umbrella):

- [DMF App Admin Account Drift Audit and Realignment Plan 2026-05-14](docs/plans/DMF%20App%20Admin%20Account%20Drift%20Audit%20and%20Realignment%20Plan%202026-05-14.md)
  — per-app audit + workaround inventory; surfaced 4 distinct drift shapes
  (username-only / username+password / password-only htpasswd / none) across
  Authentik/AWX/Forgejo/NetBox/Zot/LibreNMS.
- [DMF Internal Service DNS Migration Survey 2026-05-14](docs/plans/DMF%20Internal%20Service%20DNS%20Migration%20Survey%202026-05-14.md)
  — survey + classification (workload-type × caller-location two-axis bin
  matrix). Enabled by ADR-0023.
- [DMF In-Cluster Ansible Runner Pod Implementation Plan 2026-05-14](docs/plans/DMF%20In-Cluster%20Ansible%20Runner%20Pod%20Implementation%20Plan%202026-05-14.md)
  — 910-line implementation handoff for the runner-pod spike (Phase 1
  done in `dmf-infra@ff36ee8`; Phases 2–4 ready for fresh-agent pickup,
  ~3–4 hours estimated).

**ADR cascade this session:**
- [ADR-0023](docs/decisions/0023-internal-service-dns-for-cross-app-wiring.md)
  Accepted — cross-app HTTP wiring defaults to cluster-internal service DNS
  for pod-to-pod callers; ansible-from-control-node callers use public URL
  derivation as a transitional pattern (§Scope amendment 2026-05-14 after
  run-11 discovery of the caller-location distinction).
- ADR-0024 queued (post-runner-spike) — "configure-stage ansible runs
  in-cluster". When that lands, ADR-0023's §Scope caveat collapses.

**Override list — retired 2026-05-23.** The historical 6-flag tax was an
`aliyun-123` artefact. `g2r6-foa9` (wizard-fresh) ran `bootstrap-configure`
with **zero** `-e` overrides and reached `failed=0`. `vault_bootstrap_admin_username`
is seeded from the SOPS bundle by `dmf-env/bin/bootstrap-secrets.sh:926`
and exported consistently to all roles via `run-playbook.sh`, so the live
admin user matches role-default resolution on every fresh greenfield. The
audit playbook `audit-admin-identities.yml`
(`dmf-infra/k3s-lab-bootstrap/playbooks/`) confirms no drift across 5 apps.
Last remaining flag `awx_control_node_ssh_privkey_path` is a workstation-
path drift (not credential drift), deferred to a separate followup. See
[`docs/handoffs/DMF App-Admin Drift Realignment Handoff 2026-05-23.md`](docs/handoffs/DMF%20App-Admin%20Drift%20Realignment%20Handoff%202026-05-23.md)
for the audit findings and 698 refactor closure.

**Strategic next steps (operator-tagged for next session):**
1. Runner-pod spike Phases 2–4 — wrapper, Job/Pod template, openbao-session
   mounted-mode, end-to-end 698 test
2. Internal Service DNS migration execution (per the survey doc)
3. Investigate ntfy notification skip in 110-authentik
4. Stale-breakglass hygiene check (hetzner-lab file may contain
   aliyun-123-compatible creds — surfaced when fixing
   `get-passkey-enrollment-url.sh` resolver)

### ✅ ADR-0021 approle-reconciler verified on g2r6-foa9 (2026-05-23)

The "Tier A blocker, Option C selected (2026-05-13)" framing was stale.
Implementation landed `dmf-infra@209601d` on the same day the brief was
written; g2r6-foa9 was bootstrapped 2026-05-21 with the code in place;
2026-05-23 convergence pass verified the chain end-to-end. Tier A
bootstrap-correctness gate is closed; AC-5 separation holds on the live
cluster.

**Verification matrix** (all green):

| Probe | Result |
|---|---|
| `vertical-security/100-openbao.yml` rerun | `failed=0`; `Verify approle-reconciler login on rerun path` ok |
| `vertical-resilience/100-object-storage-credentials.yml` rerun | `failed=0`; idempotent (no AppRole recreation, no secret-id rotation); ClusterSecretStores Ready; ExternalSecrets created in 6 namespaces |
| New `verify-openbao-identity-model.yml` | `failed=0 changed=0`; 3 identity logins; 5 AC-5 deny probes (ops-admin/policy-reconciler/approle-reconciler each blocked from paths outside their charter); 2 ClusterSecretStores Ready; 6 ExternalSecrets Ready across openbao-system/awx/netbox/authentik/forgejo/zot |

**Step 5 mystery** (pre-2026-05-13 post-seed log showed `changed:` for an
op ops-admin shouldn't be able to do) is **closed** — artefact of an
execution context before the session-separation refactor was fully in
place. Live probes on the current code path (`6f6cee8`+ then `209601d`+)
cannot reproduce it; the 2026-05-23 verifier is the second independent
confirmation that AC-5 holds.

**New verifier playbook** ships at
`dmf-infra/k3s-lab-bootstrap/playbooks/verify-openbao-identity-model.yml`
— read-shaped, modelled on `verify-oidc-admin-bridge.yml`, exercises the
AC-5 matrix + ESO positive checks. Tagged `vertical-security`.

**Wired into `bootstrap-verify.yml` 2026-05-23** (`dmf-infra@596b28b`)
alongside `audit-admin-identities.yml` (vertical-security) and
`verify-oidc-admin-bridge.yml` (vertical-identity, ADR-0024 PR2). The
existing resilience-verify import is preserved. End-to-end run on
g2r6-foa9: `ok=86 changed=3 failed=0` (no skip-tags needed after the
follow-on fixes below). Two findings surfaced and were retired in the
same session — see ✅ block below.

### 🧹 Session-end small-fixes sweep (2026-05-23)

End of a long convergence session. Three of the followups flagged in
today's various handoffs landed; one was reframed after investigation.

| Item | Status | Commit |
|---|---|---|
| Authentik runtime-token finding (verifier Secret-name default drift) | ✅ closed | `dmf-infra@f434e8a` |
| Forgejo OAuth2 provider missing DMF groups scopemapping | ✅ closed | `dmf-infra@f434e8a` |
| `unseal-openbao.sh` — `err()` called before definition at line 120 | ✅ closed | `dmf-env@729f4ef` |
| `awx_control_node_ssh_privkey_path` "drift" | 🟡 not actually a drift | — |
| `audit-admin-identities.yml` preamble noise (Django/shell_plus output in `stdout_lines`) | 🟡 still open | dmf-infra (off-limits this turn) |
| AWX shadow superuser `<operator-user><...>` (likely OIDC-created) | 🟡 still open | investigation only |

**On the SSH-path "drift":** investigation showed
`awx_control_node_ssh_privkey_path: /Volumes/<operator>/secure/awx-control-node.privkey`
in g2r6-foa9's inventory is **not** broken under the Option-2 bundle-dir
decision. Option 2 scoped to `DMF_BOOTSTRAP_BUNDLE_DIR` only; the
encrypted USB volume `/Volumes/<operator>/secure/` remains mounted for
OpenBao break-glass (`openbao_key_path`, `openbao_juicefs_mount_path`,
`eso_openbao_breakglass_file` all still point there). The wizard
derives the privkey path via `$(dirname "${OPENBAO_BREAKGLASS_DIR}")`
which yields the correct value at wizard-time. No followup needed —
the entry in the App-Admin Drift Realignment handoff
("§What's still owed #1") was a misdiagnosis; superseded by this note.

**Transcript-hygiene reminder for next session:** wrapper scripts that
echo SSH targets (e.g. `unseal-openbao.sh --status`) print the
control-node public IP by design. Per dmf-cluster-access §0, those IPs
are operator-identity tokens that shouldn't enter agent transcripts.
Twice this session I echoed cluster IPs inadvertently
(`cat inventories/g2r6-foa9/hosts.ini` early on, and the unseal-openbao
status check later). Either run such wrappers in a separate terminal,
or pipe output through a filter that masks the SSH-target line. Add to
the dmf-cluster-access skill as a "don't" pattern in the next touch.

### ✅ OIDC bridge verifier — two findings closed (2026-05-23)

Wiring `verify-oidc-admin-bridge.yml` into `bootstrap-verify.yml`
surfaced two issues. Both retired via `dmf-infra@f434e8a`:

1. **Verifier K8s-Secret-name default drift.** The verifier hardcoded
   `verify_authentik_runtime_secret` default to `authentik-runtime`,
   but the authentik role default for `authentik_runtime_secret_name`
   is `authentik-env` (matches the ExternalSecret target). On
   g2r6-foa9 the verifier was looking at a Secret name that didn't
   exist; the actual `authentik-env` Secret contained the
   `AUTHENTIK_BOOTSTRAP_TOKEN` correctly. **Fixed:** default brought
   in line with the role.

2. **Forgejo OAuth2 provider missing `DMF groups` scope mapping.**
   Every other OAuth2 provider in
   `roles/stack/operator/authentik/templates/blueprints/20-app-providers.yaml.j2`
   (NetBox, LibreNMS, Grafana, DMF Console, Zot) included the
   `DMF groups` scopemapping in its `property_mappings`. Forgejo did
   not, so ops-admin group claims wouldn't have reached Forgejo on
   OIDC login. **Fixed:** scopemapping reference added; 110-authentik
   re-applied the blueprint via the Authentik worker CLI.

Verifier output after fixes:

```
OIDC admin bridge verified:
  - operator '<operator-user>' is a member of 'ops-admin'
  - 7 applications provisioned (6 OAuth2, 1 SAML)
  - all OAuth2 providers expose the 'DMF groups' scope
```

`bootstrap-verify.yml` now runs `failed=0` end-to-end on g2r6-foa9
including the OIDC bridge play.

This finding is also illustrative for the unified-app-admin-helper
plan in progress (claude-bottom pane) — the Secret-name drift is the
exact failure mode that helper would make structurally impossible by
sourcing `<app>_runtime_secret_name` from inventory at call time
instead of hardcoding a verifier-side default.

**Recovery procedure for pre-2026-05-13 clusters** (none currently live):
the Implementation Brief §6 is retained as active reference. Required if
any pre-2026-05-13 OpenBao instance needs the `approle-reconciler`
identity added against a revoked-root state — privileged Shamir-backed
ceremony per `dmf-openbao-unseal` skill, or full rebuild.

**Detailed handoff:**
[`docs/handoffs/DMF OpenBao AppRole-Reconciler Live Verification Handoff 2026-05-23.md`](docs/handoffs/DMF%20OpenBao%20AppRole-Reconciler%20Live%20Verification%20Handoff%202026-05-23.md).
Original 2026-05-13 design docs carry supersession banners pointing here.

### 🔄 Tier A Phase 3 — code done, aliyun-123 deploy in flight (2026-05-12)

Phase 3 (vertical-resilience playbooks) landed via 11 commits across
dmf-infra (438af59..03c1e17), dmf-env (f907a80 + the
[`init-wizard.sh` ship](docs/handoffs/DMF%20Tier%20A%20Phase%203%20Wrap%20%2B%20Init%20Wizard%20%2B%20aliyun-123%20Greenfield%20Handoff%202026-05-12.md)),
and dmfdeploy (a23ef7b, f11269a, 52411ed, 07cb2be, 6d7dabc). The
canonical Phase 3 record is the
[Phase 3 implementation handoff](docs/handoffs/DMF%20Tier%20A%20Phase%203%20Implementation%20Handoff%202026-05-12.md);
the cross-track session wrap (Phase 3 + wizard + aliyun-123) is in the
[Phase 3 wrap handoff](docs/handoffs/DMF%20Tier%20A%20Phase%203%20Wrap%20%2B%20Init%20Wizard%20%2B%20aliyun-123%20Greenfield%20Handoff%202026-05-12.md).

**`init-wizard.sh`** shipped in `dmf-env/bin/` — interactive greenfield
env wizard. Five in-flight bugs (charset, SOPS tmp-name, YAML quoting,
SIGPIPE, zombie-write) all fixed in sequence; five v2 hardening items
captured in [TODOS §init-wizard.sh follow-ups](TODOS.md) (manifest
stub, terraform scaffold, group_vars completeness, tf-apply.sh tfvars
discovery, .sops.yaml hygiene).

**`aliyun-123` deploy** is mid-flight. Wizard + b2-buckets + manifest
hand-fix done; **next step is `bin/tf-apply.sh aliyun-123 apply
-auto-approve -lock=false`** (TL;DR step 3 of the
[deploy quickstart](docs/runbooks/dmf-deploy-quickstart.md)). On
success, continue with pre-seed → unseal → seed-bao → export-vars →
post-seed → bootstrap-verify per the runbook.

### 🔄 Agentic harness — K1-K4 landed, first shift halted (2026-05-12)

Pane-2 track in parallel with the Phase 3 work. Forgejo issue
substrate + orchestrator commands + preflight/postflight guards
landed (commits 39e8b0d, b9d04f5, b42543e, 784e69c). First
`/agentic-run` shift halted on preconditions guard — see
[Agentic Harness First Shift Halt 2026-05-12](docs/handoffs/DMF%20Agentic%20Harness%20First%20Shift%20Halt%202026-05-12.md)
and `docs/agentic/decisions-open.md` for the 6 seed decisions awaiting
resolution.

### ✅ Aliyun — SLB backend registration fix landed (2026-05-11)

Public-lane SLB `dmf-traefik-slb` (`47.87.134.99`) had zero backends.
Two distinct defects, both aliyun-specific, both now fixed in playbook
+ live:

1. **Tailscale's `ts-input` chain dropped Aliyun internal-service
   replies.** The chain's CGNAT `100.64.0.0/10` drop covers IMDS
   (`100.100.100.200`), internal DNS, and OpenAPI VPC endpoints
   (`100.100.0.0/16`). CCM panicked on STS-token refresh ~6h after
   start and CrashLoopBackOff'd. Fix: per-host systemd unit
   `tailscale-aliyun-internal-allow.service` (bound to
   `tailscaled.service`) inserts a `RETURN` rule for `100.100.0.0/16`
   above the CGNAT drop. Installed via
   `dmf-env/tasks/aliyun/firewall.yml`.
2. **Aliyun CCM v2.14 excludes nodes with the legacy
   `node-role.kubernetes.io/master` label** from LB backend selection.
   k3s auto-applies the label to every server-mode node →
   control-plane-only cluster has zero eligible backends. Fix: post-CCM
   step in `dmf-env/tasks/aliyun/ccm.yml` strips the label. (Caveat:
   k3s re-applies on every server restart; re-run the playbook to
   restore. Two follow-up options noted in the handoff.)

**ADR:** [`docs/decisions/0019-tailscale-cgnat-vs-cloud-internal-services.md`](docs/decisions/0019-tailscale-cgnat-vs-cloud-internal-services.md).
**Handoff:** [`docs/handoffs/DMF Aliyun SLB Backend Registration Fix Handoff 2026-05-11.md`](docs/handoffs/DMF%20Aliyun%20SLB%20Backend%20Registration%20Fix%20Handoff%202026-05-11.md).

Configure stage still pending (from prior handoff).

### ✅ Aliyun — pre-seed → post-seed validated end-to-end (2026-05-10, evening)

**Layer 6 apps deployed on aliyun.** Full bring-up reproducible. Pre-seed +
seed-bao + post-seed all `failed=0`. Configure stage **NOT yet run**.

**Session handoff** with everything in detail (cluster state, what
happened across the day, where to pick up):
[`docs/handoffs/DMF Aliyun Pre-Seed to Post-Seed Live Validation Handoff 2026-05-10.md`](docs/handoffs/DMF%20Aliyun%20Pre-Seed%20to%20Post-Seed%20Live%20Validation%20Handoff%202026-05-10.md).

**New plan sections (§22 → §27)** in
[`docs/plans/DMF Bootstrap Pre-Seed Blocker Fix Implementation Plan 2026-05-09.md`](docs/plans/DMF%20Bootstrap%20Pre-Seed%20Blocker%20Fix%20Implementation%20Plan%202026-05-09.md):

- §22 — seed-bao temp-root via Shamir quorum for `secret/platform/*` writes
- §23 — narrow metadata-read for the post-seed boundary guard
- §24 — Authentik installs before its OIDC consumers
- §25 — per-app conventional username in `secret/apps/*/admin`
- §26 — three-way username consistency (install role ↔ seed-bao ↔ consumers)
- §27 — Zot htpasswd via `htpasswd -inB -C 10` CLI (passlib + bcrypt 5.x
  silently corrupts hashes)

**Env name finalised:** `aliyun-frankfurt → aliyun` (paths, content,
inventory, role variables). Historical handoff/review docs retain
`aliyun-frankfurt` as a record. Operator scripts in `dmf-env/bin/`
across the board now accept an optional `[ENV_NAME]` first positional
arg (mirrors `run-playbook.sh`): `unseal-openbao.sh`,
`get-admin-cred.sh`, `get-passkey-enrollment-url.sh`,
`bootstrap-operator-approle.sh`, `rotate-approle-secret-id.sh`,
`provision-nodes.sh`.

**Runbook:** [`docs/runbooks/dmf-deploy-quickstart.md`](docs/runbooks/dmf-deploy-quickstart.md)
has TL;DR + detailed sections; `RUNBOOK_TIMEOUT=5400` and
`tofu ... -lock=false` (JuiceFS-via-SMB doesn't honour POSIX flock)
documented.

**Next step:** Configure stage —
`RUNBOOK_TIMEOUT=5400 bin/run-playbook.sh aliyun ../dmf-infra/k3s-lab-bootstrap/bootstrap-configure.yml -e baseline_update_apt_cache=false`.

---

### ✅ Aliyun Frankfurt — Phase A blockers cleared, ready for first apply (2026-05-08, late afternoon)

**Audit + implementation pass complete.** Two-agent collaboration: Claude wrote the readiness review and questions doc; Qwen-Coder implemented Phase A items #1–#7 from the review. No live ECS yet (operator confirmed "no rush").

**Review:** [`docs/reviews/dmf-aliyun-readiness-review-2026-05-08.md`](docs/reviews/dmf-aliyun-readiness-review-2026-05-08.md) — 4 blockers + 11 security findings + Phase A/B backlog + 11-step rollout procedure. Self-contained for a freshly cleared agent.

**Questions resolved:** [`docs/questions/aliyun-rollout-open-2026-05-08.md`](docs/questions/aliyun-rollout-open-2026-05-08.md). Q1=B (separate AppRole per env), Q2=A (parametrize unseal-openbao.sh — Phase B), Q3=two-pass Tofu guard, Q4=fix-then-apply (operator-confirmed). N1: `openbao_url` copy-paste bug across envs flagged for post-bootstrap.

**Phase A landed (Qwen-Coder, 15:55–16:07):**
- `dmf-infra` (033d438): `219-host-verify.yml` hcloud block gated `when: cluster_ingress_provider_tasks is search('hetzner_ccm')`.
- `dmf-env` (31f6a78): `bootstrap-secrets.sh` env-aware (hcloud OR alicloud) for schema/init/seed-bao/export-vars; `run-playbook.sh` swapped to `bootstrap-secrets.sh export-vars`; `tasks/aliyun_slb.yml` real Alicloud CCM install (no longer a stub); two-pass Cloudflare A-record guard in Tofu module; `vswitch_id` output added; openbao_secrets.yml copy-paste role_id removed with post-bootstrap instructions.
- `dmf-env` (806b6bd): `.sops.yaml` populated with real age pubkey (Phase A #6) — both hetzner-arm and aliyun blocks reference `age1x0gntu7fy3vu7uh4mdgvl2n52wwfmd35wulh0g620ce964stj9rqnhjc8z`; TODOs removed.
- `dmf-env` (cbccf8b, earlier): full aliyun scaffold — manifest, Tofu module, inventory group_vars, `tf-apply.sh` Alicloud branch, `.sops.yaml` recipient block.

**Phase B still open** (does not block first apply):
- `unseal-openbao.sh` parametrization for env arg
- Implement `tasks/aliyun_security_group.yml` (currently stub; Tofu module already declares SG inline so non-blocking)
- Append aliyun section to `dmf-env/DEPLOYMENT.md`
- Skill `dmf-openbao-unseal` §0 update post-Phase B #1

**Future direction captured (not in scope):** [`docs/plans/dmf-multi-provider-resource-selection-future-direction-2026-05-08.md`](docs/plans/dmf-multi-provider-resource-selection-future-direction-2026-05-08.md) — eventual goal of per-resource provider+instance+pricing chooser from bootstrap and dmf-cms; Tofu module split into `<provider>/<role>` is the infra prerequisite. Deferred until env #3 demands it.

### ✅ Bootstrap Provision/Configure split — Tier 1 implementation complete (2026-05-08, morning)

**All static scaffolding landed.** No live cluster to test against — credential-grep gate and syntax-check are the verification.

**dmf-env:** `bin/bootstrap-secrets.sh` (7 subcommands: init, doctor, export-vars, seed-bao, seed-awx-control-node-ssh, status, rotate), `.sops.yaml` scaffold, SOPS/age setup docs in `docs/initial-data-gathering.md` §2b.

**dmf-infra:** 4 new bootstrap wrappers (`bootstrap-provision-pre-seed.yml`, `bootstrap-provision-post-seed.yml`, `bootstrap-configure.yml`, `bootstrap-verify.yml`); `lifecycle-provision.yml` refactored to compatibility wrapper importing the new wrappers; `lifecycle-configure.yml` converted to doc stub; `site.yml` comments corrected; 219-host-verify retagged with `bootstrap-preflight`.

**Role defaults — all credential-context `default('changeme')`/`default('admin')`/`default('dev')` removed across 9 role files:** forgejo, forgejo-bootstrap, netbox, netbox-sot, grafana, zot, awx, awx-integration, cms. Credential-grep gate: **zero hits**.

**Tier 2 (live-cluster work) is now bundled with the aliyun rollout** above — fresh cluster will exercise seed-bao, the new wrappers, and the env-aware bootstrap-secrets path end-to-end.

### ✅ Move 1 Gate 2 — Catalog launcher pivot validated (2026-05-06)

**`media-launch-nmos-cpp` runs end-to-end via AWX.** Path A pivot away from in-cluster
ServiceAccount auth landed cleanly; catalog model (NetBox `ipam.Service` parent
`dmf-traefik`, `lifecycle:bootstrapped` → `lifecycle:active` tag flip) works.
See [`docs/plans/Move 1 Gate 2 - Pivot to Path A for Catalog Launchers 2026-05-06.md`](docs/plans/Move%201%20Gate%202%20-%20Pivot%20to%20Path%20A%20for%20Catalog%20Launchers%202026-05-06.md)
for the full plan + cleanup inventory.

**Job 285 (`media-launch-nmos-cpp`) succeeded:** NetBox tag taxonomy auto-created,
service registered with parent `dmf-traefik`, K8s namespace + ConfigMaps + PVC +
StatefulSet + Deployments + Services applied, NetBox tag flipped to `lifecycle:active`.

Image push closed Item #13 on 2026-05-07; ConfigMap schema fixes (Item #15) made
pods reach Running and the Query API return HTTP 200.

### Earlier this session: Cluster redeploy completed (Path Y, 2026-05-06)

**Move 1 Gate 2 SSH credential incident** — Resolved via complete cluster redeploy.
See incident review at [`docs/reviews/dmf-move1-gate2-ssh-credential-incident-2026-05-05.md`](docs/reviews/dmf-move1-gate2-ssh-credential-incident-2026-05-05.md).

**Path Y redeploy complete.** All phases executed:
- ✅ Phase 1: Local history rewrite (dmf-infra)
- ✅ Phase 2-4: Forgejo mirror + Tofu destroy + OpenBao re-init
- ✅ Phase 5-7: New keypair (ADR-0016), Tofu apply, bootstrap
- ✅ Phase 8+: AWX deployment (640-awx.yml manually run)
- ✅ Phase 9: AWX integration (693-awx-integration.yml manually run)

**Path A confirmed** as the AWX execution model — see
[`docs/decisions/0016-awx-control-node-ssh-via-cloud-init-and-openbao.md`](docs/decisions/0016-awx-control-node-ssh-via-cloud-init-and-openbao.md).
Private SSH key for control-node access lives in OpenBao, never in tracked files.

### In-flight work

- **Item #15 (nmos-cpp ConfigMap schema) — DONE 2026-05-07.** Images in Zot,
  pods reach Running, Query API returns HTTP 200. Three root-cause bugs fixed
  in `dmf-runbooks/roles/nmos-cpp/`. See
  [`docs/handoffs/DMF Item 15 NMOS ConfigMap Schema Fix Closure 2026-05-07.md`](docs/handoffs/DMF%20Item%2015%20NMOS%20ConfigMap%20Schema%20Fix%20Closure%202026-05-07.md).
  (Operator may cycle pods via `media-finalise-nmos-cpp` / `media-launch-nmos-cpp`
  for testing — namespace + ConfigMaps + PVC are preserved by design.)

- **Phase 5 cleanup of the pivot plan — completed/obsolete 2026-05-23.**
  ADR-0025 Lane B uses AWX Container Group pod placement and target-namespace
  RBAC. The old `spec.ee_pod_spec_override`/`awx-runner-sa` fragment is kept
  only as a historical disabled comment block.

- **Public-push prep (Path A)** — paused at decision gate. See
  [`docs/handoffs/DMF Move 2 Closure + Path A Handoff 2026-05-04.md`](docs/handoffs/DMF%20Move%202%20Closure%20+%20Path%20A%20Handoff%202026-05-04.md)
  for what's done and the four open questions (repos in scope, GitHub
  destination, license, Forgejo posture). Independent of Move 1 timing.

### Outstanding follow-ups (tracked, not blocking Move 1)

- **NetBox inventory CIDR fix — obsolete 2026-05-23.** Media launcher
  playbooks no longer SSH to the control node and no longer need a hardcoded
  `inventory_hostname` → private-IP mapping. No `k3s_node_ip` NetBox custom
  field is needed for the media-launcher path.
- **Bootstrap-seed shared catalog tags via 691-netbox-sot.** Currently
  the nmos-cpp role auto-creates them on first provision (idempotent).
  Move taxonomy seeding to bootstrap so it's a platform invariant.

### Pending decisions blocking >1 repo

- License choice — applies to every in-scope public repo.
- GitHub namespace — needs to be set before remotes go live anywhere.
- Forgejo-vs-GitHub primary — affects how each repo's push workflow
  is documented across CLAUDE.md / agent files.

### Known divergences

- Operator passkey Authentik superuser flip planned but not yet applied
  (prior handoff still open). Per-app admin propagation audit also partial.
- Existing `DMF NMOS Registry + Crosspoint Demo Plan 2026-05-04.md` Phase 3
  superseded by the Move 1 + Catalog task; header note added in-place.
- `Move 1 Gate 2 Fix - AWX EE Pod Service Account Mount.md`,
  `Move 1 Gate 2 — AWX Integration + Launch NMOS.md`, and
  `Move 1 Gate 2 — Trials and Tribulations 2026-05-05.md` plans are all
  superseded by the 2026-05-06 Path A pivot plan (banners added 2026-05-07).
- `roles/nmos-cpp/` was authoritative-source-of-truth-moved from `dmf-media`
  to `dmf-runbooks` on 2026-05-06; the umbrella component table + `bin/generate-status.sh`
  now treat `dmf-runbooks` as the 6th component repo (registered 2026-05-07).

<!-- HUMAN-END -->

---

**Boot ritual reminder:** when starting a session in any DMF repo,
`git fetch && git pull` the umbrella, read this file, read the most recent
handoff in `docs/handoffs/`, run `git status` in any component repo
(`../dmf-*`) you're about to touch, and ask the user before modifying any
component repo with dirty state.
