# Backlog membership baseline — 2026-08-29

**This file is the pin.** It records the backlog's membership state at a named
instant so a later reconciliation can tell an *intended* change from *drift*.
Regenerating the baseline means writing a new dated file, not editing this one.

| | |
|---|---|
| measured | 2026-08-29, against `dmfdeploy/dmfdeploy` |
| umbrella `main` | `606da4f` |
| supersedes | the `106/106` baseline in the milestone-reconciliation work order (rev 16) |
| regenerated by | sequence item **A5** |

**Why it was regenerated.** The previous baseline was taken at 106 open issues and
again at 125; both predate the issue creation and closure of 2026-08-27/28. Any
issue filed after a baseline is taken re-stales the partition, the oracle, the
moved count and the drift baseline — so the baseline is retaken *after* all
authorised issue creation, and only then.

Issues created 2026-08-27/28 that this baseline counts exactly once, with their
GitHub creation timestamps (UTC):

| issue | created | state |
|---|---|---|
| `#458` | 2026-08-27T14:49:26Z | closed |
| `#460` | 2026-08-27T15:45:18Z | open |
| `#463` | 2026-08-28T10:31:44Z | closed |
| `#465` | 2026-08-28T10:36:43Z | closed |
| `#467` | 2026-08-28T10:40:28Z | closed |
| `#469` | 2026-08-28T10:56:55Z | open |
| `#470` | 2026-08-28T11:02:49Z | closed |
| `#474` | 2026-08-28T18:42:36Z | open |

Net still-open additions: **`#460`, `#469`, `#474`**. Nothing was filed on
2026-08-29, so no issue postdates this baseline's own measurement date.

## Reproduction

Every figure below comes from one snapshot:

```bash
gh issue list --repo dmfdeploy/dmfdeploy --state open --limit 500 \
  --json number,title,milestone,labels > open-issues.json
```

`gh issue list` excludes pull requests, so the totals are issues only.

## 1. The partition — acceptance check 8

Every open issue sits in **exactly one** state:

```
milestone ∈ {v0.1-polish, episode-001-capture, v0.2}
  XOR (no milestone ∧ platform-debt)
  XOR hygiene-exempt
```

| state | count |
|---|---|
| `v0.1-polish` | 23 |
| `episode-001-capture` | 8 |
| `v0.2` | 65 |
| no milestone ∧ `platform-debt` | 17 |
| `hygiene-exempt` | 1 |
| **total open** | **114** |

23 + 8 + 65 + 17 + 1 = 114. **Holes: 0. Overlaps: 0. Check 8 PASSES.**

Supporting invariants, all clean at this baseline:

| invariant | result |
|---|---|
| issues in a milestone other than the three | 0 |
| `platform-debt` carried by a milestoned issue | 0 |
| no-milestone issues without `platform-debt` | 1 — `#118` only, the hygiene bot's own report |
| open issues missing `component:*` or `workstream:*` | 0 |
| `freeze-1` in any milestone | 0 |

`hygiene-exempt` has exactly one carrier, `#118`. It is an exemption of last
resort; a second carrier appearing without a recorded decision is drift.

## 2. The Phase-1 oracle — acceptance check 4

**Definition.** The oracle is the set of ROADMAP Phase-1 prerequisites named by
the work order: `{#348, #379, #343, #344}` ∪ the §3a move list ∪ the issues filed
2026-08-26 under operator authorisation. Fourteen issues, pinned here:

| issue | oracle source | state at baseline | scheduling home |
|---|---|---|---|
| `#232` | §3a | OPEN | `v0.2` — **not** `episode-001-capture` |
| `#291` | §3a | OPEN | `v0.2` — **not** `episode-001-capture` |
| `#292` | §3a | OPEN | `episode-001-capture` |
| `#319` | §3a | OPEN | `episode-001-capture` |
| `#326` | §3a | CLOSED | `v0.2` |
| `#327` | §3a | OPEN | `episode-001-capture` |
| `#343` | check 4 | OPEN | `v0.1-polish` — **not** `episode-001-capture` |
| `#344` | check 4 | CLOSED | `v0.1-polish` |
| `#348` | check 4 + §3a | OPEN | `episode-001-capture` |
| `#379` | check 4 + §3a | OPEN | `episode-001-capture` |
| `#383` | §3a | CLOSED — Gate A | `episode-001-capture` |
| `#405` | §3a | OPEN | `episode-001-capture` |
| `#449` | filed 2026-08-26 | OPEN | `episode-001-capture` |
| `#452` | filed 2026-08-26 | OPEN | `episode-001-capture` |

**Open oracle members: 11.** Closed: `#326`, `#344`, `#383`.

`episode-001-capture` holds exactly 8: `#292`, `#319`, `#327`, `#348`, `#379`,
`#405`, `#449`, `#452`. Set-equality against the 11 open oracle members therefore
**does not hold** — see §3.

## 3. The `blocks:episode-001` drift baseline

**Carriers at this baseline: zero.** The label exists (created at A1) and has
never been applied to an issue. The saved view over it is empty.

The work order's §1 rule is that a milestone is an issue's *exclusive* scheduling
home, and cross-cutting episode-001 blocking is carried by this label — never by
a second milestone. Under that rule, an open Phase-1 prerequisite whose
scheduling home is another milestone should carry the label. Three do not:

| issue | scheduling home | why it is in the oracle |
|---|---|---|
| `#232` | `v0.2` | §3a listed it for `episode-001-capture`; the move was not applied |
| `#291` | `v0.2` | §3a listed it for `episode-001-capture`; the move was not applied |
| `#343` | `v0.1-polish` | named directly in check 4; correctly homed, so the label is the only carrier available |

**This gap is recorded, not closed.** Closing it means either moving `#232`/`#291`
out of `v0.2` — a membership decision that belongs to A1, which has completed and
whose narrowing to 8 members may have been deliberate — or applying the label,
which fixes the *population* of the very set a future drift check compares
against. The rule for what earns the label is one of the inputs acceptance
**check 10** is still missing, and check 10 is explicitly not started. Deciding
it inside a baseline regeneration would make the baseline self-certifying.

So the baseline records the label as **empty, with three known candidates**. A
later run finding these three labelled is an *intended* change; finding a fourth
is drift.

## 4. The moved count — acceptance check 7

| | |
|---|---|
| parked destination | `platform-debt` label, no milestone |
| population at baseline | **17** |
| members | `#8`, `#13`, `#141`, `#146`, `#158`, `#159`, `#171`, `#302`, `#303`, `#304`, `#305`, `#309`, `#409`, `#419`, `#451`, `#460`, `#469` |

The 2026-08-25 proposal estimated ~35 infra items for this destination from
title-level triage. The applied figure is 17. The difference is not loss: that
estimate was explicitly labelled approximate and was superseded by full-body
classification during A1.

Six of the 17 also carry `freeze-1` — `#8`, `#13`, `#141`, `#146`, `#158`, `#159`
— which is consistent: a Freeze-1 item must be in no milestone, and parking is
where an unscheduled item lives.

## What this baseline does not do

It does not implement acceptance **check 10**, the scheduled drift
reconciliation. Check 10 needs a versioned oracle (this file supplies one), an
exact rule for what issue text constitutes an episode prerequisite, a durable
PASS/FAIL report, an operator alert on mismatch, and recorded remediation. The
work order authorises one bounded implementation change for it, on the condition
that the exact file, function, inputs, outputs, failure behaviour and one
verification command are named *before* work starts. That naming has not
happened, so check 10 remains its own decision.
