# DMF v0.2a WP-D Kickoff Handoff

**Date:** 2026-07-07
**Status:** ✅ **WP-E landed** (dmf-cms#24 + dmf-infra#43, merged to `main`; codex 2-round PASS) — operator-gated the three AWX writes with the C5 quartet, RP-initiated (end-session) logout, Settings→avatar nav. **Next up: WP-D (G26).** Issue **#185** stays open (D/C/L1/L2/L3 remain for v0.2a).

## For the next session

Pick up **WP-D — NetBox-derived per-instance MXL endpoints (dmf-cms, G26)**, the critical-path Monitor piece for v0.2a (without it you cannot *see* lifecycle state or a switch — codex P2). The full inline spec is in `docs/plans/DMF v0.2 Media-Native Console Demo Plan 2026-07-06.md` under "### WP-D" (that doc's framing is superseded by `docs/plans/DMF v0.2 EBU Facility-Orchestration Re-anchor Plan 2026-07-07.md`, but its WP specs remain the implementation detail). It adds `GET /api/media-workloads/{instance}/mxl/status` and `/mxl/preview` behind `_require_media_workloads_access`, sourcing per-instance sidecar coords from NetBox custom fields — **the security surface is the whole point**: `sidecar_base_url(svc)` must validate `cluster_service`/`cluster_namespace` as DNS labels, then apply **tight allowlists (codex P1)** — namespace in a configured set defaulting to `{mxl}`, port in a set defaulting to `{9000}`, and `cluster_service` must equal *that specific scoped instance's own release/service name* (concrete identity, not app family) — so a NetBox writer stamping arbitrary coords can't turn the console into an in-cluster proxy; compose `http://{svc}.{ns}.svc.cluster.local:{port}` byte-for-byte per the dmf-promsd contract, any failure → None. Fetches are **hardened (codex P3)**: short timeouts, response byte caps (~256KB JPEG / 32KB JSON), JPEG SOI (`\xff\xd8`) check before proxying as `image/jpeg`; the public payload leaks **no** coords/URLs/IPs (only a boolean `live_view`); degraded is **200 content** (`available:false, reason:...`), **404 only** for not-in-scope (scope-parity with `clear_for_deployment`). Reuse the WP-E patterns already in the tree: scope-checked instance lookup, the C5/audit shape, and the dmf-cms test harness (note: `uv run` writes an incidental `uv.lock`, now gitignored; async endpoints need a context-managed `TestClient` for `app.state.operations`; `dev_groups` sets the effective role). Boot ritual reminders: this is **feature-branch work → run `bin/mxl-sync-check.sh`** (never run this session); branch dmf-cms from **fresh `main`**; **codex-crosscheck via agent-bridge** before landing (codex live at tmux role `codex`); PRs arm auto-merge at open but are held by **REVIEW_REQUIRED** (safe to open when final, no `hold` needed). Umbrella doc work (IA §3 clarification + WP-E-done plan updates) sits on branch **`docs/v02-ebu-reanchor`**, pushed, not yet PR'd.
